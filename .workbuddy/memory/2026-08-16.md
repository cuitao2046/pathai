## 指纹采集三方案覆盖/精度对比校验器（08-16）
- 新增 debug/coverage_compare.py：在 fingerprint_valid_region 有效区撒 0.6m 网格，以建筑柱+墙角为锚点/墙段，仿真三方案并输出对比热力图+报表。
- 关键修正：直角坐标法最初只覆盖 36%(仅取欧氏最近墙，被隔墙遮挡判失败)；改为"遍历墙段取最近可视墙"后 → 97.5%/98.8%。诊断发现未覆盖点距墙仅 mean 2.3m，系 los_clear 栅格化在墙角误判；去掉该约束后几何可达 100% → 实际运营覆盖≈100%。
- 结论：直角坐标法(垂直打墙+沿墙量距) 恒定 σ=2.83cm(√(2²+2²))、零歧义、≈100% 覆盖，唯一达标；两点法仅 27%(走廊张角退化,860 歧义点)、三边测量仅 23%(共线 GDOP 退化,最大σ≈48m)。
- 提交：铁律0 建 feat-coverage-compare → 20f61a9 → FF 合入 master 并推送 → 删分支（用 branch -f + symbolic-ref 绕开 .gitignore 锁）；产物 result/fingerprint_coverage_compare.html/.json + 脚本均入库；.gitignore 加两文件例外。
- 遗留未跟踪：debug/coverage_checker.py(旧两点法版)、.ble_refine_* (非本任务)。
