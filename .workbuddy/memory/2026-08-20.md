# 2026-08-20 工作日志

## 信标坐标不一致根因调查（08-20，用户"为什么会出现坐标不一致？是渲染图编辑没同步吗？"）
- 用户猜测方向正确：渲染图拖拽编辑未同步全部字段。证据链：
  ① 生成脚本（gen_trilateration_plan_routes.py:218、gen_beacon_plan.py）产出时 plannedCoordinates=coordinates 自洽 → 不一致非生成所致；
  ② render_interactive.py deployEndDrag(680-715) 拖拽结束仅写回 entry.b.coordinates（698 行），plannedCoordinates/sourceNodeId/snapDist_m/locationDesc 全部保留加载旧值；导出 deployExport 深拷贝 b 原样落盘 → coords=编辑后实际位置，planned/sourceNodeId=规划旧值，两者脱节；
  ③ 3e06bbc 首次入库时 7 枚硬伤 coords 已与 planned 不一致（后续 0 变更）→ 不一致形成于入库前的部署编辑阶段；
  ④ coords 实际位置印证"拖拽到现场安装点"：008/032/033 均命中 F1-LB-0049 lobby（最近 TI=交叉口55/53），021 在 F1-CR-0048 走廊中部，与声明的交叉口11/45/47/71 均不符；TR-F1-003/004/005 的 planned x 恒=-16.61 是"原始规划贴墙点"（同一条竖直走廊墙、y 间隔 2m 的贪心补点），部署时被拖走未同步——非垃圾值。
- 附加缺口：deployAddBeacon 新增 BK-M-* 信标 rec 无 plannedCoordinates 字段（设计缺口）。
- 工程修复方向（待用户确认）：拖拽写回时同步置空/重吸附最近源节点更新 planned/sourceNodeId/locationDesc；或 QA 校验 coords↔sourceNodeId>5m 告警兜底。

## 修复落地：渲染图拖拽同步 + QA 校验器（08-20，用户"启动修复"）
- 分支 feature-beacon-drag-sync（master ac51267 起），工程师+QA 双角色验证后合入：
- **src/rendering/render_interactive.py +19 行**：deployEndDrag 拖拽写回增加"人工调整语义同步"——原规划信标（有 sourceNodeId）拖走后备份 originalSourceNodeId/originalSourceNodeType/originalLocationDesc 再置空 sourceNodeId、sourceNodeType='manual_adjusted'、plannedCoordinates=null、snapDist_m=0，locationDesc 统一「人工调整 fkF（x,y） · 原规划: …」；BK-M-* 人工信标拖拽仅更新坐标+描述（「原描述:」前缀）；entry.info=deployBuildInfo(entry.b) 重建并同步 DOM data-info。deployAddBeacon 新增信标补 plannedCoordinates: null。
- **新增 src/tools/validate_beacon_deployment.py**（正式 QA 校验器）：R1 coords↔sourceNodeId>5m ERROR（带 snapDist_m 一致性门槛，与 audit 硬伤判定对齐）、R2 coords↔planned>5m ERROR（同门槛）、R3 人工信标 planned 应为 null WARN、R4 楼梯语义区无楼梯关键词 WARN、R5 必填字段 ERROR；任一 ERROR→exit 1。
- **验证**：unittest 50/50 全绿；校验器对当前脏数据报 R1=4+R2=7=11 ERROR（与 docs/17 硬伤清单逐条一致）+R4=13 WARN；三组构造用例（健康/修复后/回归）全通过；JS node --check + 18 项功能断言 PASS。QA 建议放行。
- ⚠️ **已知遗留**：①fingerprint_grid_routes.json 从未入库且磁盘缺失 → 重生成 HTML 丢 648 路线指纹点（652→5），本次**不提交 HTML**（保留入库版），待输入恢复后重渲染；②BK-M 多次拖拽 locationDesc 累积「· 原描述:」后缀（轻微文案冗余，非阻断）。
- 数据修正（ble_deployment.json 7 处硬伤）仍待用户拍板 4 枚 coords 方向（方案A 以 sourceNodeId 回填 vs 方案B 反向改源节点），未在本分支处理。

## 指纹路线网格恢复 + HTML 重渲染（08-20，用户"继续修复#1指纹网格问题，#2暂缓"）
- 分支 feature-recover-fingerprint-routes（master 1851913 起）。#2（BK-M 多次拖拽 locationDesc 累积后缀）按用户指示暂缓。
- **根因**：result/fingerprint_grid_routes.json（测试路线指纹采集网格）从未入库且磁盘缺失 → 重渲染 HTML 丢 647 指纹点（652→5）。
- **恢复方案**：入库版 HTML（git HEAD）完整含 647 指纹点 → 新增 `src/tools/recover_fingerprint_routes.py` 反向提取（正则解析 .layer_fingerprint data-info + circle cx/cy → GEOX 反变换：ox=-83.7935398305038, oy=27.00297849202474, scale=7.0, marginX=50, marginY=30, titleH=46, perFloor=929）→ 组装与 fingerprint_grid.json 同构 JSON。647 点 = F1 611 + F2 36，全部落在开放空间（0 封闭房间），回环误差 0.000。
- **重渲染**：`python -m src.rendering.render_interactive`（⚠️ 直接跑脚本报 No module named 'src'，必须 -m 方式）。新 HTML：fingerprint 引用 652、layer_fingerprint 精确 647、manual_adjusted=2、plannedCoordinates:null=1（上分支 JS 修复随渲染生效）、指纹点 id 与入库版 647/647 一致。
- **.gitignore** 加白名单 `!result/fingerprint_grid_routes.json`（35 行）。
- **QA 验证**：JSON 结构/坐标语义/HTML 一致性/unittest 50/50 全过；脚本 --html 重跑逐字段可复现。工程师修两处：DEFAULT_HTML 指向 result/floor_layout_v9_interactive.html（原指向已删 debug 备份）+ _ELEM_RE 改转义感知正则（防未来撇号数据截断）。
- 教训：grep -c 'layer_fingerprint' 会多算 1（CSS 定义），精确数元素须用正则 `<g class="layer_fingerprint" data-info=`。

## 数据修正（方案B）+ 渲染图坐标同步增强（08-20，用户"部署模式移动/添加后所有坐标需同步"+"008是移动后的coords"）
- 分支 feature-beacon-coord-sync（master 019fea7 起）。用户确认方案B：coords 是部署模式移动后的真实位置。
- **数据修正 src/tools/fix_beacon_coords.py**（白名单幂等）：7 枚硬伤按 coords 为真值修正——A 族 4 枚 plannedCoordinates:=coordinates + 重吸附最近交叉口（008→F1-TI-0265 交叉口55、021→F1-TI-0255 交叉口45、032/033→F1-TI-0263 交叉口53，locationDesc 追加「· 原:」追溯）；B 族 3 枚 planned:=coords、snapDist_m=0。校验器重跑 R1=0/R2=0/ERROR=0(exit 0)，审计硬伤 7→0，54 枚零改动（git diff 逐字段确认），幂等（二次运行 diff 空）。
- **渲染图 render_interactive.py**：Python 注入 TOPO_NODES={floor:[{id,label,type,coordinates}]}（仅 intersection/doorway，F1=213/F2=132）；新增 deployResnap(entry,x,y,fk)（同层≤10m 最近节点吸附→归属节点；未命中→sourceNodeId=''/manual_adjusted）；deployEndDrag 拖拽后 plannedCoordinates:=[x,y]（同步而非置空）+重吸附+original* 备份（含 originalPlannedCoordinates，resnap 全量覆盖 locationDesc 多次拖拽不重复追加）；deployAddBeacon 新增 plannedCoordinates=[x,y]+落点 resnap。全局模式拖拽分支未动。
- **QA 校验器 R3 调整**：人工信标 plannedCoordinates 必须存在且与 coords ≤0.5m，否则 WARN「人工信标坐标未同步」（旧规则"应为 null"废弃）。
- **验证**：unittest 50/50；QA 独立复核 7 枚/54 枚、snap 豁免（008 距交叉口55=5.06m>5m 靠 planned=coords 豁免 R1，判定合理接受）、deployResnap 冒烟 5 组、R3 三组用例、node --check 全过。
- ⚠️ QA 遗留建议（非阻塞）：吸附阈值 10m 与 R1 阈值 5m 不一致，未来信标 7~10m 吸附会以 snap=0 豁免 R1 掩盖远距离归属——建议后续统一阈值或 R1 校验真实距离。
- ⚠️ 存量 8 枚 BK-M-* 无 plannedCoordinates → 新 R3 报 8 WARN（预期提示，需经新渲染图重新导出后消除）。

## 08-20 下午：10m 吸附阈值与多坐标字段分析（决策征询，未改码）
- **#1 吸附阈值 10m 量化结论**：61 枚信标→最近 intersection/doorway 距离 max=7.30m、>5m 仅 7 枚、3~5m 6 枚、≤3m 48 枚；现状归属距离（coords↔sourceNodeId）>3m 11 枚（>5m：BK-01-027=8.2m、BK-01-031=5.58、BK-01-021=5.27、BK-01-008=5.06）。
- **⚠️ 新发现（校验器失效）**：方案B planned:=coords 后 planned_d=0、snap=0 → snap_ok 恒真 → R1/R2 的 5m 漂移检测对 5m 级归属完全失效（BK-01-008 距交叉口55 5.06m 被静默放过，QA 判定"合理接受"实际掩盖了 5m 吸附事实）。
- **10m 阈值真实副作用**：把走廊中间 route_corridor/route_fill/人工信标（BK-01-038/042/043、BK-M-1-002/003、BK-TR-F1-003/005 等 7 枚）误吸附到 5~7m 外门/交叉口；BK-01-036 距 TD-0087=4.80m vs TI-0287=4.99m 归属歧义；facility 类（电梯口 BK-01-001/002 归属 F1-TF-0011）不在 TOPO_NODES 候选，拖拽会被误吸成门口/交叉口。
- **推荐**：阈值收窄 3m（48/61 正常吸附，13 枚落空转 manual_adjusted 其中 8 枚本就该 manual）；候选按类型加权（同距离优先保持原 type，facility 不参与重吸附）；R1 改为直接比 coords↔sourceNodeId（去 snap_ok 豁免），否则 5m 级漂移永不报错；BK-01-008/032/033 三枚方案B 5m 级吸附需单独复核 coords。
- **#2 多坐标字段结论**：coordinates=部署真值；plannedCoordinates=规划基准（方案B后拖拽/新增同步=coords）；snapDist_m=吸附偏移；original*=运行时备份（JSON 0 枚）。49/61 枚 coords≠planned（多为有意偏移，中位 1.48m，max 6.44m）。不能直接删 planned（R2 依赖+审计痕迹+8 枚人工缺 planned 靠 R3 提示未同步），建议方向A双字段收敛（保留+明确职责+R1 修真实距离）或方向B单字段（删 planned+snap，R2 删除，改动面大）。已用 AskUserQuestion 征询用户拍板。

## 08-20 晚：方案B单坐标 schema + 3m 吸附落地（用户拍板：阈值3m + 方案B单字段）
- **render_interactive.py**：deployResnap 10m→3m 类型加权（同类型≤3m 优先→兜底任意类型≤3m→manual_adjusted；facility/facility_entrance 豁免不重吸附）；deployEndDrag/deployAddBeacon 不再写 plannedCoordinates/snapDist_m，首次拖走备份 originalSourceNodeId/Type/LocationDesc + originalPlannedCoordinates(=旧coords)；deployCollectBeacons 加载时防御性剥离旧字段（旧 planned≠coords→originalPlannedCoordinates）；info 面板「吸附偏移」行→「原始坐标」行。
- **validate_beacon_deployment.py**：删 R2/R3；R1 改为直接比 coords↔sourceNodeId>T_SRC_DRIFT=3.0（去 snap_ok 豁免——旧逻辑 planned:=coords 后 snap_ok 恒真，5m 级归属永不报错）；规则集 R1/R4/R5。
- **fix_beacon_coords.py 重写**：阶段1 schema 迁移（61 枚全迁移，41 枚写 originalPlannedCoordinates，20 枚无——原 planned==coords 或本无）+ 阶段2 3m 漂移重归属（11 枚超差）：BK-01-008/032/033/036→manual_adjusted（5m 级方案B吸附，3m 内无节点，追溯进 originalLocationDesc）；BK-01-012/021/030/034→doorway（兜底）；BK-01-027/031/035→同类型 intersection 重归属（交叉口5/17/33）。幂等二次 diff 空。
- **验证**：校验器 ERROR=0（R1/R4/R5）；deployResnap 冒烟 6/6（同类型保持/兜底doorway/manual/facility豁免/3.0边界命中/3.1落空）；node --check 全过；HTML 重渲染 61 信标 647 指纹点。unittest 48/50——2 个 golden 失败为既有问题（test_pipeline_golden 房间解析 F1-RM-0058 等，与本次无关，未动解析代码）。
- ⚠️ 遗留：BK-01-008/032/033/036 改判 manual_adjusted 需现场复核（离交叉口 4.3~5.1m，可能挂走廊中间）；unittest golden 需 REGEN_GOLDEN 刷新（与本次无关）。

## 08-20 1700+：蓝牙信标语义优化 TODO 文档（docs/18）
- 用户要求把上一轮「三点定位信标语义配置」回答整理为优化 TODO 文档。
- 新建 `docs/18-蓝牙信标语义优化TODO.md`：五层模型（L2 定位层真值底座 / L4 语义层决策核心）+ 逐字段配置规范（带当前达成状态 ✅⚠️❌）+ 三点定位语义要求（GDOP/TxPower/minor 分段/dir 信标/指纹双轨）+ 语义事件清单 + 5 大常见坑（前 4 坑项目已修）+ 8 条 TODO（P0: adjacentRooms 经过提示 / 4 枚人工信标现场复核；P1: 走廊段语义 / locationDesc 语音化 / voicePrompt 覆盖；P2: riskLevel 分级 / unittest golden 刷新 / 指纹语义独立化）。
- 未 commit（纯文档新增，待用户确认是否一并合入；按铁律 0 应走新分支，用户未要求合入，先留工作区）。

## 08-20 1800+：三点定位覆盖点 ID 命名空间区分（feature-coverage-id-namespace → master）
- **问题**：「三点定位覆盖」图层点位直接复用指纹采集点 `id`（FP-1-Rxxx），与指纹网格图层命名撞车——点击覆盖点时详情面板显示的 id 与指纹点一模一样、无法区分；虽无 DOM id 冲突（info_attr 只写 data-info 不写 SVG id），但语义命名相同。
- **修复（src/rendering/render_interactive.py）**：新增 `_cov_id(fp_id)` 辅助（FP-1-R001→CV-1-R001，非 FP 开头→`CV-<原id>`）；覆盖点 `data-info.id` 改用 CV- 前缀、`sourceFingerprintId` 回链原指纹点、详情面板标题/行同步（显示「覆盖点 CV-1-R001 · 源指纹 FP-1-R001」）；注释说明二者语义不同（采样位置 vs 可见信标可视性评估）。
- **验证**：HTML 重渲染；覆盖点 647 个全部 CV- 前缀、指纹点 647 个全部 FP-、撞车集合空、647/647 覆盖点带 sourceFingerprintId 回链；py_compile OK。
- **测试安全**：tests/ 仅 golden_stats.json 的 "coverage" 是 GeoJSON 结构统计（walkable 覆盖率），与渲染层无关；无任何测试 import render_interactive 或比对 HTML；既有 2 个 golden 失败（test_pipeline_golden 房间解析 F1-RM-0058）与本次无关。
