# 2026-09-21 工作日志

## docs/15 采购预算：新增「与全量部署（全楼层）方案的预算对比」
- 需求：用户要求在采购预算 md 中增加与全量部署采购预算的对比。
- 做法：新增**第四节**（原四/五顺延为五/六），含 3 小节：4.1 预算对比表、4.2 维度对比表、4.3 结论与建议；版本行改为 2026-09-21；第六节依据补一条引用。

### 关键数据（供后续文档复用）
- **全量部署 = docs/08 §五 方案 C**：324 部署 + 5 备件 = **329 枚**（F1 204 / F2 120，全楼 walkable 3563 m²）；≥3 可见覆盖 F1 96.6% / F2 98.1%（1m 网格仿真）；无需指纹采集；工期 2~3 天；配套 ≈¥480（VHB ¥300 + 勘测 ¥180）。
- **docs/08 §6.2 信标小计**（329 枚）：档1 ¥45→14,805 / 档2 ¥35→11,515 / 档3 ¥27→8,883。
- **统一到 docs/15 口径**（信标 + A/D 固定项 ¥973，用现有终端）：
  - 档1 15,778（3.79× 方案I）/ 档2 12,488（3.61×）/ 档3 9,856（3.41×）
  - 由方案 I(71) 扩展到全量需追加 258 枚：+11,610 / +9,030 / +6,966
- 结论写进文档的核心判断：**多花钱买的是覆盖范围而非精度**——纯三点在线 1.33m/35.9% 不靠加密信标解决（docs/16 敏感性分析），亚米级须靠指纹库+PF/EKF/IMU 融合。建议先方案 I 验证，再按需追加。

### 注意
- docs/08 与 docs/15 的备用件口径不同（08 用 +5 备件，15 用 +10 备用），对比时保留 329（08 口径）并在表中注明，未强行统一。
- 本次**只改 md**，docx 未同步（用户此前约定 md 为工作稿、定稿后再生成 docx）；若需同步须改 `debug/gen_reports_docx.py` 后重跑。
- 文件已落盘，**未提交**（铁律0），待用户确认。

## 新建 docs/21-工具材料采购预算申请-报批版.md（面向导师审批）
- 需求：去掉方案 II，让导师理解"选方案 I = 投入产出最大化（投入=采购成本，产出=充分验证可行性）"，生成新版文档推动预算审批。
- 做法：**不动 docs/15**（保留内部完整版含方案 II 与全量对比），新建报批版：
  - 一节摘要（申请 ¥4,168 / 可压缩下限 ¥2,890 / 交付产出 / 验收指标）
  - 二节投入产出论证：2.1 目标是"验证可行性"非"全楼交付" → 2.2 四项已达标指标表 → 2.3 边际投入产出表（71→108/71→329/S910/iPhone 四个加钱项逐一证明"不加钱"）
  - 三节清单（仅方案 I，71 枚）、四节合计三档、五节产出与验收里程碑表、六节风险应对（**方案 II 从并列预算降级为"风险预案"**，≤¥999 不列入本次）、七节依据
  - 推荐档1 Minew（¥4,168），并把档3 作为"可压缩下限"给出，便于导师批一个区间。

### 本次新实测数据（重要，可复用）
- 运行 `debug/eval_ble_deployment.py`（复用 `src/tools/refine_beacon_placement.py` 无线模型：TxPower −10 / RSSI@1m −50 / n=3.5 / 可见阈值 −85 dBm），评估现行 **61 枚** 在 `fingerprint_grid_routes.json` 上的表现：
  - **≥3 可见覆盖 = 97.7%**（F1 596/611 = 97.55%、F2 36/36 = 100%）
  - 退化点 138/647 = 21.3%（F1 132 + F2 6），其中 GDOP>3 共 26 点、三点外扩 >1m 共 112 点、both 0
  - 走廊双侧覆盖：F1 12 条中 6 双侧/2 单侧/4 无；F2 10 条中 1/1/8（F2 仅 9 枚偏少）
  - 其余：F1 墙角违例 17、柱违例 11；F2 墙角 5、柱 0
  - 产物 `result/beacon_deployment_evaluation.json`（result/* 在 gitignore 白名单外，未入库）
- ⚠️ `eval_ble_deployment.py` 里 PLANS 的 B/C 项（routes60 / refined_max20）文件已不存在会被 skip，只输出 A（现行 61 枚）——正常。

### 工具环境（再次确认）
- Bash 本轮**又坏了**（`dirname: command not found`，且 `rm` 走 safe-delete shim 也失败 → 删除静默无效）。→ 本轮数据核实用 PowerShell 调 venv python（stdout 不回显，靠脚本写文件 + Read），删文件用 PowerShell `Remove-Item` + Glob 复核。
- Bash 的可用性是**间歇性**的：能跑就直接跑（如上一轮的 git 操作），报 `dirname not found` 就立刻切 PowerShell，别反复重试。

## 关键交付物状态
- `docs/21-…报批版.md`（新建）、`docs/15-…md`（本轮加第四节）——**均已落盘，未提交**（铁律0），待用户确认后走分支合入。

## 修正：激光测距仪 2 台 → 1 台（用户质疑"为何需要两个"）
- 用户问"为何需要两个激光测距仪"。查证结论：**2 台站不住脚**，且与项目既定口径冲突——
  - `docs/08` §6.3 原文：激光测距仪 **1 台 ¥150**，"与信标部署、指纹采集、地图勘测三环节**共用，不重复采购**"；§5.4/§6.3 亦记"共用激光测距仪"精简决策（`docs/项目迭代日志.md` 第 148 行）。
  - 技术上：直角坐标法在同一测点分两次测量（先沿墙距、再垂直打墙），一台顺序完成即可，不需同时工作；转台刻度环读角与机器台数无关；热备份可由耗材/换电池覆盖，不值得单列一台。
  - "2 台"是早期估算残留，我此前只写入 docs/15 与 docs/21，与 docs/08 不一致。
- **修正为 1 台**（保留"可选加购第二台 +¥169"的备注弹性），金额联动：
  - A 类合计 693 → **524**；固定项 A+D 973 → **804**
  - 方案 I（71 枚）合计：档1 **3,999** / 档2 **3,289** / 档3 **2,721**（含终端 5,999 / 5,289 / 4,721）
  - 方案 II（108 枚）合计：5,664 / 4,584 / 3,720（含终端 7,664 / 6,584 / 5,720）
  - 全量（329 枚）合计：**15,609 / 12,319 / 9,687**；③/① 倍数 3.79/3.61/3.41 → **3.90/3.74/3.56**；追加 258 枚金额不变（差值与固定项无关）
  - docs/21 摘要：申请额 ¥3,999、可压缩下限 ¥2,721
- 同步文件 3 个：`docs/21-…报批版.md`、`docs/15-…md`（版本行已注明本次口径修正）、`debug/gen_reports_docx.py`（同步数字，防以后重生成时口径错）。
- ⚠️ 遗留不一致（待定稿处理）：`docs/15` 的 docx 与 `debug/gen_reports_docx.py` **尚未包含第四节「与全量部署对比」**（脚本里没这个章节），定稿出 docx 时需补。

## 再次精简：去掉采集终端（¥2,000）与充电宝（¥100）
- 用户指示"不需要购买充电宝和信号采集终端"。C 类（iPhone ¥2,000）与 D3（充电宝 ¥100）移出采购清单，改为"C. 采集终端与充电（不列入采购）"说明节。
- **顺手补上一条自洽论证**：指纹库所需的距离真值由 A 类测距设备现场测得，**不依赖手机传感器**——因此不买专用 iPhone 不影响数据质量（此前把 iOS 的 accuracy/txPower 说成"必需"是过度依赖，与"测距仪提供真值"重复）。
- 金额联动（固定项 = A 524 + D 180 = **704**）：
  - 方案 I：档1 **3,899** / 档2 **3,189** / 档3 **2,621**
  - 方案 II（108 枚）：5,564 / 4,484 / 3,620
  - 全量（329 枚）：**15,509 / 12,219 / 9,587**；③/① 倍数 3.98 / 3.83 / 3.66×；追加 258 枚金额不变（11,610 / 9,030 / 6,966）
  - 去掉"含新购终端"两列（终端不再采购）
- 同步 4 处：`docs/21-…报批版.md`、`docs/15-…md`（版本行注明 ①②③ 三次修正）、`debug/gen_reports_docx.py`、**并重跑脚本重新生成两份 docx**。

## 本轮补齐：docs/15 docx 现已含第四节
- 把「四、与全量部署（全楼层）方案的预算对比」**补进 `gen_reports_docx.py`**（原"四、可选升级"→五、"五、依据"→六），解决 md/docx 结构差异。
- 重跑生成两份 docx，并用临时脚本校验：**旧口径残留 = 无**（693/973/2,000/4,168/…/含新购终端 全部清零），docs/15 docx 章节完整（一~六，含第四节），docs/14 docx「五、资源需求」已同步新数字。
- ⚠️ **工具教训（新增）**：
  - `PowerShell Remove-Item` 再次**静默失败**（连 `-LiteralPath` 单文件都不删）；本轮最终用 `python -c "import os,glob; [os.remove(f) for f in glob.glob(...)]"` 删除成功。
  - `PowerShell ... *> file` 重定向写出的是 **UTF-16**，`Read` 会报 "Cannot display content of binary file"；要校验内容就让 Python 自己 `write_text(encoding='utf-8')` 再 Read。
  - 结论：**本环境删除文件用 Python `os.remove`，看输出让 Python 写 UTF-8 文件**。

## 补充：D 类辅助耗材「使用说明 + 采购必要性」论证
- 需求：用户要求补 D 类辅助耗材的使用说明，即**明确采购的必要性**（导师会问"这 ¥180 是干什么的"）。
- **先实测取数**（不凭想象写）：写临时探测脚本读 geojson / ble_deployment.json，得到支撑论证的硬数据：
  - 单层外廓（rooms+columns 稳健范围）**≈ 76 m × 118 m**（F1/F2 一致）→ A1 有效幅面 821×574 mm，**1:150 旋转后恰好容纳**（1:100 装不下 118m）
  - 信标挂装：`mountType` = **wall 54 + door_frame 7**；`installHeight` 全为 **2.2 m**；`batteryModel` 全为 **CR2477**
  - 信标编号体系：前缀 `BK-01` / `BK-02` / `BK-M`；`semanticTag` 43 base + 10 fill + 8 manual
  - 路线网格采集点：F1 611 + F2 36 = **647**；transform ox=-83.7935, oy=27.0030, scale=7, perFloor=929
- **写入的论证结构**（D1/D2/D3 各一段，统一用"怎么用 → 不采购的后果 → 单价说明"三段式）：
  - **D1 编号标签 ¥30** = 数据可追溯性的最低硬件要求：71 枚信标贴物理编号对 `beaconId`；647 个采集点贴已采/未采标记。不买则台账与实物失联、漏采无法核对 → 指纹库点位完整性失去可核查性。用可移除贴纸因教学楼是校方资产不得留痕。
  - **D2 A1 图纸 ×2 ¥100** = 现场作业的基准载体：按图核对信标点位/挂高、记录 647 点行进顺序、标注"图纸—实建差异"回写 `surveyRequired`。不买则失去与数字地图的对照基准。纸张不可被手机替代（可任意标注/不怕断电/A1 一眼看全整层）。
  - **D3 杂项 ¥50** = 免破坏安装与点位可调：美纹胶带/无痕胶用于**试挂**（临时固定→测覆盖→定永久位）、扎带/魔术贴用于免钻孔固定。不买则 54 枚墙面 + 7 枚门套的信标挂高无法稳定保持 2.2 m（RSSI 失真、与 docs/16 仿真无法对齐），且"优先微调点位"风险应对失去物理前提。
  - 小结：**D 类 ¥180 占总额 4.6%**，但决定 ②③④ 阶段作业可行性与数据可信度。
- 同步 4 处：`docs/21-…报批版.md`（D 节重写 + 使用环节列 + 章节编号注）、`docs/15-…md`（同上，另加"环节编号 ①~⑥ 对照"注 + §4.2 耗材行补注）、`debug/gen_reports_docx.py`（D 节表加两列 + 3 段说明段）、**docs/14 资源需求新增 1 条耗材必要性 bullet**；重跑脚本重新生成两份 docx。
- 校验：docs/15 docx 段落 44 / 表 9，新增关键词全部命中（D1/D2/D3、76m×118m、mountType、surveyRequired、BK-01-xxx、CR2477、4.6%、免钻孔），旧口径零残留（2,000/693/973/3,999），`**` 残留 0；docs/14 docx 段落 68，新增 bullet 到位。
- 顺手修掉两处**章节交叉引用错误**（docs/21 D1 里的"第四节"应为"第五节"；docs/15 里指向不存在的"第六节"改为直接描述 `surveyRequired` 字段）。
- 🧹 临时探测/校验脚本（`_probe_dim*.py/txt`、`_chk_d.py`、`_chk_out*.txt`）已全部删除；`debug/` 下仍存**他人/早前遗留**的 `_assess_out.txt`、`_bench2.py`、`_bench_timing.py`、`_bench_out.txt`、`_pf_out.txt`、`_tri_out.txt`、`_audit_out.txt`（本轮未动，等用户决定是否清理）。
- 本轮 Bash **完全不可用**（非仅 `dirname`：`ls`/`grep` 也 not found，`rm` 走 safe-delete shim 报 `/safe-delete-common.sh: No such file`）→ 全程 PowerShell 调 venv python + 脚本写 UTF-8 文件 + Read；删除用 `python -c "os.remove"` + Glob 复核。

## 关键交付物状态（截至本轮）
- `docs/21-…报批版.md`、`docs/15-…md`+`.docx`、`docs/14-…docx`、`debug/gen_reports_docx.py` —— **均已落盘，未提交**（铁律0）。
- `docs/21` 尚未生成 docx（用户约定 md 为工作稿、定稿后再出 docx）——等用户定稿。

## docs/08-实施路线图.md 新增「七、施工作业影像记录建议」
- 需求：更新 docs/08，增加施工作业图片记录建议，**重点说明哪些环节需要拍照留用**。
- 结构调整：在「六、阶段验收总览」后插入新第七节（原七「安全与隐私设计」→**八**，原八「核心设计决策」→**九**）；并在任务 1.1/1.3/1.4 的**交付物**末尾挂上影像记录引用，在第六节表后加一行指向第七节。
- 新第七节（7 小节）：
  - 7.1 三条原则：**可复现**（画面须有固定参照物或拍摄板，文件名不算）/ **可对账**（编号与 `ble_deployment.json`、指纹采集表一一对应）/ **可合规**（校区是未成年人密集场所）
  - **7.2 必拍环节清单（核心）**：9 个环节分 P0/P1/P2 三级 + 对应任务 + 拍摄对象 + 张数要求 + 留用用途
    - **P0（工程不可逆信息，100% 覆盖、禁止抽采）**：① 信标逐枚安装（每枚 ≥3 张：正面编号可辨 / 环境全景 / 固定细节；61 枚 ≈183 张）② 建库测距读数（每测点 ≥1 张）③ 图纸—实建差异（每处 ≥2 张，回写 `surveyRequired` 的依据）
    - **P1（允许抽采，安全节点须全覆盖）**：无障碍要素与高风险节点（每要素 ≥2 张）、指纹采集点环境（抽采 ≥20%、安全节点 100%）、覆盖验收过程（批次 ≥3 张）
    - **P2（不作验收硬指标）**：既有安全隐患、环境变更留档、用户实测过程（录像需单独审批）
  - 7.3 拍摄规范：信标等高平视 2.2m、环境全景退 3~5m；**必须有不变参照物（柱/门框/楼层标识；家具展板绿植会移动不能当唯一参照）**；编号必须可读；**拍摄板**（D1 标签纸 + A5 白纸手写"楼层-点位-日期"，¥0）；**关闭相机地理标记**（场馆坐标属场馆资产，呼应第八节「信标安全」）
  - 7.4 命名与归档：模板 `{楼层}-{类别码}-{对象ID}-{序号}-{日期}.jpg`；类别码 BC/FP/SV/AC/HZ/EV；对象 ID 规则（信标用 `beaconId`、采集点用序号、差异/隐患用发现顺序号）；相册目录 `site-visual-records/F1|F2/{01-survey,02-beacon,03-fingerprint,04-hazard,05-change}`；**仓库只存索引表 `docs/site-visual-index.csv`（photo_file, floor, category, object_id, seq, shot_at, photographer, note），影像文件不入 git**（体积 + 授权管理，援引 11M 信标 SDK zip 的既有处置口径）
  - **7.5 合规与隐私（重点）**：先授权后拍摄（校方书面许可，明确区域/时段/用途）；避开学生、优先非课间或放学后、**拍到学生须现场立即删除底片不得延后**；含师生人像不得上公有云/项目外使用；教室门牌等与地图数据同级授权；录像从严（2.6/3.6 需单独书面知情同意）
  - 7.6 与验收挂接表（4 条验收标准 → 影像证据）+ **台账字段建议：`ble_deployment.json` 增设 `photoRef`**（建议新增，未落地）
  - 7.7 所需准备：不新增采购（复用手机 + D1 标签纸 + A1/A5 现有工具），索引表入库、原图进共享盘
- 校对：章节编号 一~九 已核（grep `^## ` 全部命中）；并把指向采购清单的编号统一写成"采购清单 D1 / 卷尺（A5）/ 激光测距仪（A1）"，避免读者不知出处。
- **影像需求量化（可复用到其他文档/汇报）**：P0 最低量 ≈ **183 张信标安装照 + 647 张测距读数照 + 图纸差异照**；即建库阶段一次性影像量级 ~800+ 张。
- `docs/08` 已落盘，**未提交**（铁律0）。

## 全量文档一致性 Review（所有 md ↔ 代码/数据实现现状）
- 需求：review 所有 md 文档，按最新方案实现调整，确保文档与代码实现现状保持一致。
- **可复用方法（四步）**：
  1. **先取数据真值**（严禁凭记忆）：读 `result/school_building_01_map_v9.geojson`、`result/ble_deployment.json`、`result/fingerprint_grid*.json`、`src/` 模块清单 + 关键源码（如 `geojson_writer.py`），得到规模/字段/参数基线。
  2. **扫描脚本**（`debug/_sweep.py`）：对 docs + 根 md 跑 17 组正则（扶梯/escalator、A\*、753、1442、324/348、48/74、plannedCoordinates、roomType、schema 1.0.0、8 篇、12 脚本、pathai_src…），输出 file:line；**排除 `result/` 与 `.workbuddy/memory/`**（历史日志按 append-only 不改）。
  3. **断言式批量修正**（`debug/_fixdocs*.py`）：每条替换带期望命中数，`re.subn` 计数不符即 FAIL 汇报——避免误改/漏改。
  4. 修完**复扫**：剩余命中逐条判定「可接受（已显式标注历史态）」或「需再修」。
- **实测基线（2026-09-21，v9.0.0）**：房间 136（F1 81/F2 55）、门 208（F1 132/F2 76）、墙 4442、柱 220、拓扑节点 465（F1 284 = room 50 + doorway 135 + intersection 78 + facility 13 + facility_entrance 8；F2 181 = 39/79/53/10/0）、拓扑边 592（F1 350）、跨层边 10；信标 61（wall 54 / door_frame 7；txPower −10、interval 300、height 2.2、CR2477 5 年）；指纹 1434 全楼 / 647 路线（F1 611 + F2 36）；riskNodes F1 68（outward_door 55 / stair_entrance 10 / elevator_entrance 3）、F2 41（31/7/3）；src **61 个 .py**（含 11 个 `__init__.py` → 50 个模块）。
- **修掉的关键不一致（30 文件、约 30 处）**：
  - **房间类型字段口径（最实质）**：`geometry.rooms[].properties` 的 `type` 是**粗粒度**（功能房间统一 `room`，公共/设施型保持 corridor/staircase/stair_lobby/toilet/lobby/elevator_lobby/infrastructure），`roomType` 与 `roomSubType` 同值且为**细粒度用途**（classroom/office/equipment/activity…）；`semantic.rooms[]` **只有 `type` + `roomSubType`（无 `roomType`）**，且 `roomSubType` 仅在功能房间上有值、公共/设施型为 null。依据 `src/io/geojson_writer.py:969`「需求⑳+1」。→ 改 docs/03 §9、docs/20、docs/11、CAD 根文档。
  - **docs/03 §9 顶层字段示例 3 处错**：`origin` 实为 `{x,y,unit}` **对象**（非 `[2019.1,1154.8]` 数组）、`generator` 实为 `src/io/geojson_writer.py`（非 parse_cad_pdf.py）、`venueName` 无空格。
  - **扶梯清理 12 处**（docs/01、04、06）：本项目试点楼无扶梯；仅保留 docs/01 §五「大型商场」通用场景 + 一条显式注记，以及 docs/02/08 中"本建筑无扶梯"的规则说明。
  - **docs/08-实施路线图 任务 2.3**：删「加权 A\* + 多目标权重向量」，改为受限 Dijkstra + 门惩罚（`DOOR_PENALTY`/`DOOR_DEFAULT_PENALTY`）+ 中间节点类型约束 + 跨层只走电梯 + 盲区剔除 + `BLIND_WALK_SPEED=0.8`。
  - **docs/02 信标网络段 + 技术选型表**：按台账实值改为 300ms / −10dBm / 2.2m / CR2477 5 年 / **iBeacon 单帧**（原文写 Eddystone 双帧、100~300ms、−12~+4dBm、锂电池）；另「楼梯扶梯两端」→「楼梯两端」。
  - **docs/08 §九 常见问题**：删已废弃字段 `snapDist_m`（改为 `coordinates` 真值 + `sourceNodeId` + `originalPlannedCoordinates` 口径）；广播 300–400ms→300ms；E5「2400mAh 锂电池」→CR2477。
  - **docs/07** 方案量级 48/66/324 → 48/74/324（与 docs/08 §三 对齐）。
  - **docs/项目迭代日志** 顶部加「2026-08-04~08-11 历史快照」警示（其 §一 概况的 docs 19 篇 / src 12 脚本 / 门 138+82 / 指纹 975+467 均为当时态，非现状）。
- ⚠️ **环境教训（重要）**：沙箱**托管 Python 3.13.12 无 shapely**，致 `tests/test_route_rules_consistency.py` 里 `compute_route_rule_extras` 被条件置 None → `TypeError: 'NoneType' object is not callable`（47 tests / 1 error 的假失败）。**跑回归必须用 `C:\Users\Administrator\.workbuddy\binaries\python\envs\default\Scripts\python.exe`**（shapely 2.1.2 / networkx 3.6.1）；该 venv 走真实 RouteGraph 分支，`test_pipeline_reproduces_golden` 需数分钟。
- Bash 仍间歇性断（`dirname: command not found`），且**跨分钟的长命令会被 SIGTERM**（测试跑到 38 项被杀）→ 长任务必须 `run_in_background`。
- 交付物：29 个文件（原记 30，实为 28 个已跟踪文件改动 + 本日志 1 个新增），**已按铁律0 分支入库**（见下节）。

## 拆分入库：三个堆叠分支（用户选定「拆两个分支」+「未跟踪文件部分入库」）
- 背景：工作区当时同时压着**两个需求**的未提交改动（上一轮采购预算 + 本轮文档 review），按铁律 0b 必须拆开。
- **采用堆叠（stacked）而非各自从 master 起 + rebase**：因为记忆里明确 rebase 会触发沙箱 git 误删 bug。堆叠 = 每个分支基于前一个分支的 tip，`git add` 范围严格按路径前缀校验（目录条目会展开为多文件，不能用计数相等校验），最终可逐个 fast-forward 入 master，全程零 rebase、零冲突。
- 校验手段：每次 `git checkout -b` 前后比对 `git status --porcelain` 必须完全一致（防误删）；提交前校验「全部暂存文件均属本次路径范围」。
- 三个分支（均已 push 到远端 `github-cuitao:cuitao2046/pathai.git`，用 `git ls-remote` 校验本地/远端哈希一致）：

| 分支 | commit | 规模 | 内容 |
|---|---|---|---|
| `feature-reference-archive` | **1042549** | 41 files, +1310 | 参考资料入库：英才计划培养方案.md、ble_refine_corridor_report.md、minibeacon V5 广播格式.pdf、docs/reference/MinewBeaconAdmin/、src/sdk/ |
| `feature-budget-docs` | **d1cc213** | 5 files, +334/-66 | 采购预算：docs/15.md+docx、docs/14.docx、docs/21.md(新)、debug/gen_reports_docx.py |
| `feature-docs-consistency` | **0c062bf** | 29 files, +961/-512 | 本次全部文档一致性 review（+ 本日志） |

- 顺序即堆叠顺序（archive → budget → review），入 master 时按此序 FF。
- **master 未动**，仍为 `8a1ee21`；三支分支待人工校验后 FF 合入（合入后按铁律删分支）。
- 未跟踪且**不入库**（用户确认）：6 个根目录调研 PDF（NavCog / iBeacon localization / p270-sato / survey-final / w4a 等）、`docs/reference/信标SDK.zip`（10.6MB，沿用既有"SDK zip 不入 git"口径）、`.ble_refine_parse.json` / `.ble_refine_corridor_walls.json`（临时中间产物）。
- ⚠️ 两点可优化（未动，等用户定）：① `docs/reference/MinewBeaconAdmin/` 与 `src/sdk/MinewBeaconAdmin/` 是**同内容两份**（各约 10.6MB），入库后仓库一次性增加约 21MB，建议后续去重留一份；② `src/` 下挂第三方 SDK 与「正式脚本仅 src/」约定略冲突。
