# 2026-08-08 工作日志

## 同步最新代码（git pull origin master，fast-forward）
- 本地 master 落后 origin/master 共 24 个提交（2c5220b..12f051f）；已 fast-forward 合入，工作树干净，0 ahead/0 behind。
- 远端这 24 个提交带来的新功能（另一环境继续开发）：
  - **骨架手动标注工作流**：`export_skeleton_template.py`(导出 SVG 模板) → `import_manual_skeleton.py`(导入) → `apply_manual_skeleton.py`(覆盖自动骨架并重建渲染图)；`c175fd5` 已重生成渲染图。
  - **导航绕行修复链**：`fix_crossing_edges.py`(穿墙/穿管井边改走道可见图绕行)；中间节点白名单化 + 孤儿门全部附属房间(1909ca3)；中间节点白名单加 doorway 修复房间不可达(0bff1ce)；TR→TD 归属校验(2f00563)；卫生间穿墙例外 + 管井不参与导航(3c6bc35)。
  - **门节点编号**：渲染图门节点显示 TD-xxxx（楼层-类型-序号），31d67a6 / 12f051f。
  - **指纹采集网格**：`generate_fingerprint_grid.py`(指南§八) + 渲染图层；`result/fingerprint_grid.json` 新增。
  - **房间覆盖**：`apply_room_overrides.py`。
  - **走廊/教室多边形重叠修复**(7476bf7)：红圈公共区域缺骨架/指纹点，F1-CR-0048 corridor 吞入合班走廊→差集保留最大块；红圈中心进入 walkable。
  - **前室切分 + 骨架剪枝 + 渲染图手动标注**(5ad965f)；**walkable 外溢修复 2F**(81f1d91)。
- `src/` 由 6 个扩至 **12 个**正式脚本；`result/school_building_01_map_v9.geojson`(最后 3c6bc35)、`result/floor_layout_v9_interactive.html`(最后 12f051f)、`result/fingerprint_grid.json` 均已随码重生成并提交，本地 pull 后即为最新，**无需重跑**。
- 记忆维护：更新 MEMORY.md 的 src 列表(6→12)、目录约定、方案迭代历史补一行(骨架手动标注工作流+导航绕行修复)。

## 路线诊断：音乐教室→组织办公未走 F1-D-0018/F1-D-0065
- 用户截图显示路径第一跳未走 F1-D-0018、倒数第二跳未走 F1-D-0065。
- 检查当前 geojson：`F1-TR-0015`(音乐教室) 出门边为 `F1-TE-0017/0018/0055` → `F1-TD-0017/0018/0102`， rooms 均含 `F1-RM-0015`，对应物理门 F1-D-0018；`F1-TR-0036`(组织办公) 进门边为 `F1-TE-0034` → `F1-TD-0065`，对应物理门 F1-D-0065。
- 重新跑 `render_interactive.py` 生成 HTML，与仓库已有文件无 diff，确认产物是最新的。
- 从 regenerated HTML 的 path-graph 跑 Dijkstra：`F1-TR-0015 → F1-TR-0036` 共 24 节点 / 146.0m，路径为 `F1-TR-0015 → F1-TD-0017 → ... → F1-TD-0065 → F1-TR-0036`，确实经过 F1-D-0018/F1-D-0065。
- 结论：用户看到的行为来自浏览器缓存或旧文件；已交付重新生成的 HTML 供验证。

## 拓扑门节点净化（改 topology.py / pipeline.py）：去除悬空 corridor-only + 重叠门
- 用户问「这两个重叠的拓扑节点是什么」+「直接去除悬空节点有什么影响？」→ 决定从源头（拓扑生成）修，而非渲染层隐藏。
- 根因诊断（重跑解析器后确认）：旧 geojson 是过期产物；当前代码已能正确建 TR↔TD 边（F1-RM-0007 等之前失联房间已恢复可达）。但仍存在两类冗余门节点：
  1. **corridor-only 门**：门 `rooms` 全是开放空间(corridor/lobby)或空 → 只连 TI，不连任何房间（51 个 F1 / 6 个 F2）。
  2. **重叠门**：同一物理开口被识别为多记录(swing+fire+opening)，投影到骨架后坐标重合（16 组 F1 / 6 组 F2）。
- 修复（`src/skeleton/pipeline.py` 的 `build_skeleton_topology` + 同步 `src/topology.py` 的 `build_floor_topology`，新增 `_merge_nearby_doors`）：
  - 门先按 center_m 合并(0.8m)，再按**投影后最终坐标**二次合并(1.0m) → 消除重叠 TD 节点。
  - 创建 TD 节点前做 `has_room` 判定：`any(rid in room_index for rid in dr.rooms)` 为假则**不建该 TD 节点**（走廊通行性由 TI↔TI 承担）。`door_node_ids` 用 None 占位被跳过的门，边构建循环统一跳过。
- 结果（新生 geojson）：F1 节点 316→241、TD 129→54；F2 节点 194→146、TD 75→43。**corridor-only=0、重叠组=0、不可达房间=0**，主连通分量 100%，跨层边 10 条不变。
- `validate_geojson.py` 校验项 #1 改写：原「TD ≥ 0.9×物理门数」会因新设计硬失败；改为校验**每个封闭房间(TR)是否存在 TR↔TD 边**（房间可经门进入）。结果 PASS。
- 路线回归：音乐教室(F1-TR-0015)→组织办公 仍首跳 F1-TD-0017、末跳 F1-TD-0048，路径有效（183.7m/28 节点），无退化。
- 注意：**实际产物由 pipeline.py 生成**，topology.py 为回退路径；两处已同步改。render_interactive.py 仅撤销了排查期的 console.log 调试（已重新生成 HTML）。
- 提交：src/skeleton/pipeline.py、src/topology.py、src/validate_geojson.py、result/school_building_01_map_v9.geojson、result/floor_layout_v9_interactive.html（master, ff-only）。
