# 2026-08-06 工作日志

## 同步代码并收尾遗留任务状态

- **git 同步**：本地 master 落后 origin/master 2 个提交，`git pull --ff-only` 完成 fast-forward 同步（无冲突、工作区干净）：
  - `900ab03` feat: 交互式地图新增「导出所选图层 SVG」按钮（render_interactive.py + HTML 重生成）
  - `d206b3d` docs: 新增 `docs/09-公共空间识别方案.md`（负空间分析+中轴线提取算法蓝图）
- **验证**：
  - 用同步后的 `render_interactive.py` 重生成 HTML，与远端产物**内容完全一致**（仅 CRLF/LF 换行符差异）；已 `git checkout` 还原工作区 HTML，避免换行符噪音进入 git。
  - 楼梯编号确认：`crossFloorEdges` 10 条全部带 `code` 且 `matchedBy:"code"`（如 `II-B1-01#ST`），GeoJSON 输出正常（任务 #23 闭环）。
- **任务列表收尾**：#10（渲染脚本早已完成）、#23（楼梯编号输出完成）、#29（提交同步完成）→ 全部标记 completed。任务列表清零未完成项。
- 本次无需提交（本地与远端已一致）。

## 公共空间识别：规划 → 实现分支

- 12:22 会话新增规划分支 `codex/public-space-recognition-planning`（提交 `8222f8f`），含 `docs/task.md`（12 任务/4 阶段拆解）、`docs/公共空间识别方案-gpt.md`（581 行）、`docs/公共空间识别方案-最终版.md`（631 行）。
- 12:24 用户要求创建实现分支 → 从规划状态（8222f8f）新建 **`codex/public-space-recognition-implementation`** 并 push（`-u` 建立 tracking，远端已建）。
- 任务拆解要点（task.md）：Phase1 几何增强（T1 Walkable Polygon 差集障碍物、T2 ElevatorLobby/StairLobby 细分）→ Phase2 骨架（T3 栅格化 Medial Axis、T4 矢量化构图、T5 Dangle 剪枝）→ Phase3 拓扑重构（T6 Junction 度数≥3、T7 门正交投影到骨架、T8 重构 topology.py）→ Phase4 集成验证（T9 解析器集成、T10 跨层适配、T11 可视化调试、T12 验收 QA）。
- 实现起点：在 `codex/public-space-recognition-implementation` 分支上，从 T1（Walkable Polygon 生成）开始。

## T1 完成：Walkable Polygon 生成（提交 c77e7f2，已推送）

- **实现**（`src/parse_cad_pdf.py`）：新增 `generate_walkable_polygons(rooms, wall_segs, stair_boxes, evtr_boxes, col_boxes, ...)`——
  - 障碍物 = 柱子 bbox(buffer 0.10m) ∪ 楼梯井/电梯井 bbox(buffer 0.05m) ∪ **封闭房间多边形**(roomType ∉ 开放空间，含楼梯间/电梯厅/管井) ∪ 墙线并集 buffer(**WALL_BUFFER=0.12m**)；
  - walkable = 公共空间多边形 \\ 障碍物，`_keep_walkable_pieces` 过滤 <0.5m² 碎片；
  - 每个开放空间房间 dict 写入 `walkable_poly_pt`（Shapely，pt 坐标）；GeoJSON 房间 feature properties 新增 **`walkablePolygon`**（MultiPolygon，m 坐标，`_walkable_geojson` 含内环=柱洞）。
- **关键坑（生成时机）**：`reconcile_facilities` 会**追加补齐/平移**的楼梯/电梯 bbox（平移复制自另一层）。若 walkable 在 `parse_floor` 生成，会漏掉补齐井道 → F1-AC-0047（社团）与 F1-ST-0006 重叠 0.96m²。**修复：调用移至 `build_geojson.floor_block` 开头（reconcile 之后）**，扣除列表与最终 GeoJSON 一致。
- **验收**（`debug/debug_walkable.py`）：F1 22 个/F2 16 个 walkable，全部"落房间内/不重叠井道/墙体扣减"通过；标准 QA `validate_geojson.py` VALIDATION PASS（无门封闭房间 0、拓扑孤岛 0）。扣减 Top：门厅 13.3m²、走道 8.4m² 等。
- 诊断教训：墙 buffer 扣减后 walkable 顶点距墙 ≈0.12m（buffer 边界），诊断"贴墙"阈值须 <0.06m，不能用 `buffer.contains`（0.12m 会误报）。
- 产物：GeoJSON 已重生成（+2.8 万行 walkablePolygon 坐标）；HTML 未变（render_interactive 不内嵌该字段）。

## T1 可视化（提交 6fd6b20）：动态渲染图新增「可通行区域」图层

- 用户要求"每一步都重新生成动态渲染图，生成完后再开始下一步"——确立工作流：**实现步骤 → 重生成动态渲染图（可视化该步成果）→ 确认 → 下一步**。
- render_interactive.py：新增 `layer_walkable`（浅绿 #A5D6A7 半透明填充、内环=柱洞仅描边、pointer-events none）；图层面板「可通行区域」开关（默认勾选）；`allLayers` 数组同步（导出 SVG 自动支持）。JS 用 node --check 校验。

## T2 完成：公共空间细分类（提交 0d00758，已推送）

- **实现**：`classify_elevator_stair_lobby()`（parse_cad_pdf.py），corridor/lobby → elevator_lobby/stair_lobby：
  - **电梯前室**：距电梯 <1.5m 且面积 ≤150m² —— 本图纸电梯井极小(~2.9m²)且直接贴走廊主段（走廊段 98~178m²），task.md 的 8~60m² 上限导致**永远漏分**，故放宽；>150m² 视为贯穿走廊/门厅保持原类；
  - **楼梯前室**：距楼梯 <3m 且面积 ≤60m²（task.md 原条件）；
  - **关键坑**：条件必须**独立判定**（电梯优先），不能取"最近井道"——电梯旁走廊段往往距楼梯更近，取最近会走楼梯分支而漏判电梯；
  - 保护：标签含"出入口"（入口门厅如"门厅无障碍出入口"）不重分类；
  - 井道位置参考 bbox + 已识别 staircase/elevator_hall 房间多边形（同源更精确）；调用在 floor_block 开头（reconcile 补齐井道后）。
- **联动修改**：topology.py OBJ_TYPE 加 `elevator_lobby→ELB`/`stair_lobby→SLB`、OPEN_SPACE_TYPES 纳入（circulation 节点）；validate NON_ENCLOSED 纳入；render ROOM_COLORS 加 `elevator_lobby #FFE0B2`/`stair_lobby #D7CCC8`；walkable 开放集合纳入前室（前室也有 walkablePolygon）。
- **结果**：F1 前室 7 个（3 电梯 + 4 楼梯）、F2 8 个（5 电梯 + 3 楼梯）；QA VALIDATION PASS；渲染图已重新生成（前室显示新颜色）。

## 渲染图图层开关演进（提交 aa5700c + 1d51c7d）

- 用户反馈"渲染图中没有看到前室颜色"→ 根因：walkable 图层（浅绿 opacity 0.55）绘制在房间**之上**盖住前室色。修复：walkable 移至**最底层**绘制 + opacity 降至 0.35（走廊无填充仍见绿色，前室颜色在顶层）。
- 用户要求"每种新增元素都在渲染图加图层开关"→ 现状：walkable（可通行区域）、elevator_lobby（电梯前室）、stair_lobby（楼梯前室）三种新增元素均有独立开关；前室 polygon class 追加 `layer_lobby_elevator`/`layer_lobby_stair`（仍属 layer_room，房间开关整体控制、专属开关单独查看）；allLayers 同步（导出 SVG 自动支持）。
- **约定确立**：此后每个任务新增的元素（骨架/中轴线/节点等）都必须配套独立图层开关（T11 可视化时同样适用）。
- **修正（提交 751bc91）**：用户问"为什么切「房间」影响电梯前室"→ 前室原本附带 layer_room class。改为**完全独立**：前室只带 `layer_lobby_elevator`/`layer_lobby_stair`；交互走 `[data-info]` 属性、导出走 `[class*="layer_"]`，脱离 layer_room 无副作用；CSS 补专属 opacity 0.85 + cursor。此后新增元素图层一律**完全独立**，不挂在既有图层下。
- **开放空间拆分（提交 e4a54e4）**：用户要求 corridor/lobby/activity 从房间层拆出——`layer_corridor`(走道 17)/`layer_lobby`(门厅 4)/`layer_activity`(活动区 2)/`layer_atrium`(中庭 0，数据暂无) 各自独立开关；「房间」开关现只控制封闭房间（教室/办公/卫生间/楼梯间/管井等）。当前图层面板开关清单：房间/走道/门厅/活动区/中庭/电梯前室/楼梯前室/可通行区域/墙体/窗户/楼梯/电梯/柱子/建筑外轮廓/普通门/门洞/防火门/拓扑节点/拓扑边/跨层连接/风险点/坡道/盲道/地面材质。

## walkable 户外含混区修复（提交 c34538f）

- **用户反馈**：T1&T2 后 walkable 包含"户外靠近墙体的地方"。诊断发现：
  - 几何上 walkable 都在 outline 内（无越界），但 outline 弥合门洞半径 1.4m 让副楼与户外小庭院被圈成同一连通块（如 F2 副楼左下方红线勾出的小月牙内含 walkable）
  - 视觉上"绿在户外"是 outline 边缘误差区的副作用
- **修复**：parse_cad_pdf.py walkable 生成后新增 outline 裁剪：
  - 复用 `from render_interactive import building_outline`
  - 房间中心过滤剔除无房间块的户外连通块 + buffer(-1.0) 剔除 outline 边缘含混区
  - mask_pt 与内存 walkable_pt 同单位做 intersection
  - **坑1**：shapely 2.x `transform(func, geom)` 把整段 coords 一次性传给 lambda，只返回首对导致变换不生效 → 改用手写 _m_to_pt 函数
  - **坑2**：MultiPolygon 未导入导致 F2 clip 块 NameError → 顶部补 import
- **结果**：F2 walkable 1285→1276m²（剔除户外小庭院 8.6m²），F1 几乎不变（2866m²）；QA VALIDATION PASS；诊断脚本 `debug/debug_walkable_outdoor.py`。

## 合并 fixed_code 修复（提交 2f651e3）

- 用户让读 src/fixed_code/CHANGELOG_FIX.md → 发现另一会话的修复清单未应用到 src/，且 fixed_code 是"我的中间版+修复"（缺 MultiPolygon import、缺 clip 最终版、含 debug print）→ 不整体覆盖，**手动合并**。
- **topology.py 3 处**：门口宽度换算 0.0529；门口连开放空间改为"归属中的+距门口≤15m 的全部"（修复两侧走廊经同一门洞互通）+ 兜底；走廊直连阈值 30→50m。
- **parse_cad_pdf.py**：新增 detect_stair_boxes()/detect_elevator_boxes() 统一检测（早期注入/门洞范围/最终 geometry 共用）；楼梯面积 3-80m²/聚类 1.5m/长宽比≤3、有编号仍拒 aspect>5（STAIR_MAX_ASPECT_CODED）；电梯 1-30m²；楼梯间 room 注入去重（<2m 只补 code）；门贴墙容差 12→14pt；LABEL_MIN_SIZE 9.0→8.5pt。
- **验证**（与 CHANGELOG 一致）：F1 楼梯 9/10 直接检出（B1-03 reconcile 补齐）、电梯 3/3；F2 楼梯 7/7、电梯 3/3；QA VALIDATION PASS；walkable 户外裁剪（F1 2865/F2 1276m²）与拓扑不受影响。
- src/fixed_code/ 保留为 untracked（另一会话产物，不提交）。
## T1.5 升级: 走道图层同步裁剪（提交 249a476）

- review fixed_code/CHANGELOG_FIX.md 公共空间/户外相关逻辑 → 发现其 T1.5 方案比我的 src 更完整。
- **旧实现缺陷**：只裁 walkable_poly_pt，但渲染走道图层(layer_corridor 等)画的是 coords_m **未裁剪** → 用户切走道图层仍看到户外走廊。
- **升级**（取自 fixed_code 方案）：
  - building_outline close_r=10（弥合 1.0m，比默认 14 更紧）
  - mask 内缩 1.2m（≥ 弥合膨胀 + 半墙厚余量）
  - 裁 walkable + **同步裁公共空间房间 polygon_pt/coords_m/centroid_m**（渲染走道图层用裁剪后坐标）
  - 整块在户外 → 清空 walkable；_largest_poly 保留最大部件
- **效果**：F1 开放空间房间 3238→2913m²（裁剪 14 个）、F2 1487→1286m²（裁剪 8 个）；walkable F1 2856/F2 1252m²；QA VALIDATION PASS。
- 教训：渲染图层用 coords_m 而非 polygon_pt——修改几何时必须同步更新 coords_m/centroid_m，否则渲染层与数据层不一致。