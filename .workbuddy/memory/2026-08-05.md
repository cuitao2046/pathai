# 2026-08-05

## 从 git 同步最新代码并重新生成产物

- `git pull --ff-only origin master` 成功（fast-forward，无冲突）。远程含另一台机器推送的 11 个新提交：
  - **目录重组**：调试脚本 `src/*` → `debug/`；`render_interactive.py` 由仓库根目录 → `src/render_interactive.py`；路径改为基于 `__file__` 自动解析（移除 `E:/code/pathai` 硬编码）。
  - **门口分类逻辑修复**：卫生间防火门直接丢弃；卫生间门洞仅保留通往公共空间者（丢弃内部隔断 opening）；`II-WR-01` 等卫生间/楼梯间带 DK 摆弧门重分类为门洞(opening)；修复 `parse_floor` 在 `doc.close()` 后访问 `page` 崩溃；补充 DK 文本实体识别 + 与旋转无关的矢量 DK 识别；删除 `render_v7.py`。
- **同步异常修复**：pull 后 `src/topology.py`、两个 `.bak` 在 HEAD 中被跟踪但磁盘缺失（checkout 未写入），从 HEAD `git checkout` 恢复，工作树恢复干净；三个核心脚本 `py_compile` 通过。
- **A-TECH-SANT 整层剔除改动仍保留**（src/parse_cad_pdf.py 的 `LAYERS_IGNORE`）。
- **重新生成产物**（用最新代码）：`result/school_building_01_map_v9.geojson` + `result/floor_layout_v9_interactive.html`。`validate_geojson.py` → VALIDATION PASS。
  - 门计数（新版逻辑下）：F1 普通门68/门洞8/防火门53（共129），F2 36/4/32（共72）；房间 54。
  - 门洞数较昨日大幅减少（F1 41→8、F2 37→4），因新逻辑丢弃卫生间内部隔断门口（仅保留通往公共空间者）。属另一台机器的有意变更。
- `render_interactive.py` 现位于 `src/`，重渲染命令：`python src/render_interactive.py`。

## 简化门洞（DK → opening）识别规则

用户反馈：卫生间通往公共空间的门洞丢失（之前 F1 仅 8、F2 仅 4）。明确**门洞 = window 图层中带 DK 矢量 strokes 文字标注的部分**，DK 标注在 CAD 中可有 4 个旋转方向（0°/90°/180°/270°，门外圈字标按室内阅读者放置），DK 识别本身已由旋转无关的 `is_dk_block` 处理。

**改动（`src/parse_cad_pdf.py`）**：
1. **移除 `_opening_in_scope` 范围过滤**——它只保留卫生间/楼梯间附近 30pt 内的 opening，**绝大部分洞口**（尤其卫生间通往过道/公共空间）都被它误丢。删除该段（连带 `OPENING_SCOPE_PT`、`toilet_polys+stair_polys` 局部使用）。
2. **`_door_is_internal_core` 首行豁免 opening**：函数首个判据改为 `if kind=="opening": return False`（门洞两侧皆通行，不算"内部隔断门"），仅对 swing/fire 检查核心内部隔断。该步让男↔女卫生间内部 DK 开门、卫生间→设备间 DK 开门等被全部找回。
3. **重写 `find_wall_openings`**：删除"对所有摆弧门中心近邻 <18pt 跳过"的全局避让（这条过去会因相邻不同门洞误删），**保留**「距 swing_centers < DK_NEAR_ARC_PT 才跳过」（避免普通门编号被误当洞口）；修复 wall 吸附分支缩进 bug（原代码无 `gap` 时墙吸附不可达，导致该分支永远死代码）；目标签名 `find_wall_openings(dk_blocks, all_segs, wall_gaps=, swing_centers=)` 简化。
4. parse_floor 调用同步更新（去掉 `window_groups`/`doors` 形参）。

**结果**：
| 楼层 | 普通门 | 门洞 | 防火门 | 合计 |
|------|-------|------|--------|------|
| F1 | 68 | **41** (8→41) | 53 | 162 |
| F2 | 36 | **32** (4→32) | 32 | 100 |

## 严格 DK 识别——按字符列验证 D/K（第 3 轮修复）

用户反馈：v9.1 仍把非 DK 开头的 window 元素误判为 DK（旧 `is_dk_block` 仅用「左 40%
有竖笔 + 中段有对向斜线」的笼统区域判定，M1524/C01224/甲1224 都可能满足）。

**改动（`src/parse_cad_pdf.py`）**：
- `is_dk_block` 重写为**按字符列逐一验证**：
  - 沿书写轴向把块内笔画聚成字符列（水平书写按 x，竖排按 y），与 `_glyph_vertical_angle` 配合覆盖 4 个旋转方向
  - 第 0 列 = D：有长竖茎 + 茎右侧有弧线笔画（不多设竖茎数上限——CAD 中 D 弧线由 2-4 段近竖直短划组成）
  - 第 1 列 = K：有长竖茎 + 同时存在上下对向斜线
  - 必须同时满足两个列的条件，且列紧邻
- 删除 `_cluster_x_columns`（旧版字符分列，被新的 `_cluster_along_axis` 取代）
- 新增 `_cluster_along_axis(feats, axis='x'/'y', gap=4.0)` 按轴聚笔画为字符列

**诊断验证**：在放开竖茎上限后，DK 识别 F1 从 52→20、F2 从 51→27（~60% 减少）。
拒绝原因分布完全是真实的非 DK 字形：`block_too_narrow`（太窄标签）、`d_no_vert`（无 D 茎）、`d_no_right_arc`（非 D，可能是 M/1/2 等）、`k_no_vert`（数字位）、`k_no_opp`（无 K 斜线配对）。

**最终门数（VALIDATION PASS，房间 53）**：
| 楼层 | 普通门 | 门洞 | 防火门 | 合计 |
|------|-------|------|--------|------|
| F1 | 68 | **9** (41→9) | 53 | 130 |
| F2 | 36 | **10** (32→10) | 32 | 78 |

门洞数大幅下降是 v9.1 的 32/22 个假阳性被正确的严格 DK 识别清除导致的——符合用户要求。

门窗数 +33/+28，全部为开门恢复。`validate_geojson.py` → **VALIDATION PASS**，房间数 54，跨层边 10。
卫生间通往公共空间的门洞已找回，男↔女卫生间内部开门、卫生间↔设备间开门也保留。

清理临时日志，无残留，`py_compile` 通过。

## 环境注意
- `fitz`(PyMuPDF) 仅系统 Python `C:\Users\Administrator\AppData\Local\Programs\Python\Python314\python.exe` 可用。
- 项目记忆已通过 git 同步（`.gitignore` 放行 `.workbuddy/memory/`），push/pull 即可跨机同步。
