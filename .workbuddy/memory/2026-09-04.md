# 2026-09-04 工作日志

## 按最新进展更新三份导师文档（docs/14 docx、docs/15 md + docx）
- 背景：三份文档内容停留在 2026-08-17，而 8-18~9-4 期间项目变化很大（信标 74→61、采集小程序建成、docs/16 亚米级方案突破）。用户要求"根据当前项目最新进展更新"。
- 进展摸底来源：memory 日志 8-20/21/27/28、docs/16/17/18/19、fingerprint-collector/README.md、git log（8-28 后仅 1 个 memory commit，master 已收敛单分支）、result 实测数据核对。
- **核实到的权威数据（供后续文档复用）**：
  - `ble_deployment.json` = **61 枚**（F1 52 / F2 9），summary.total 已同步为 61（8-27 修的不一致）；semanticTag: route_base 43 + route_fill 10 + manual_deploy 8；subType: base 21 / fill 17 / dir 13 / manual_deploy 8 / elevator_door 2。
  - 指纹网格：全楼 `fingerprint_grid.json` **1434 点**（F1 967 / F2 467，2m 间距）；路线 `fingerprint_grid_routes.json` **647 点**（F1 611 / F2 36）。⚠️ floors 是 **dict**（key='1'/'2'）不是 list，遍历取点数要按 dict 处理。
  - 8-21 密度复核：61 枚 @7m 覆盖 F1 98.9%/F2 100%，GDOP≤2 达标 91.5%/91.7%；**@5m F1 覆盖崩到 52.5%**、GDOP>3 退化 30 点（最差 12.0）。P0 补 37 枚→98 后 @5m 覆盖 100%、退化点 30→1；但用户 8-27 已回滚至 61。
  - docs/16（核心）：墙体 NLOS **恒定偏置**是主误差源，校正后 5.76m→0.38m（≤1m 占比 6.3%→81.1%）；oracle 走廊 EKF 中位 0.29m/P95 0.60m/100%≤1m；**在线分类器仅 1.33m/35.9%**（分类准确率天花板 87~91%）；组合方案（PF 粗位+双校验+EKF+BLE/IMU 紧耦合）走廊中位 0.18~0.24m、**100%≤1m**；单帧 <5ms、PF 1.3ms/步。粗位 1→0m 收益 0.7~0.9m ≫ 换信标（σ_d 2→1m）0.2m。
- **文档改动要点**：
  - docs/14：日期改 2026-09-04；2.2 改为「信标部署与语义层建设」（61 枚 + 单坐标 schema + 3m 吸附 + adjacentRooms 61/61 + 五层语义模型）；**新增 2.4 定位算法突破（docs/16 全套结论）**、2.5 指纹采集工具链建成（小程序 + 1434/647 网格）、2.7 文献调研；2.6 保留方案仿真表；问题部分新增 5m 覆盖风险、在线未达亚米级、验证范围仅限走廊 38 步、4 枚点位待复核、golden 2 个失败；下一步把「PF 粗位 + 双校验」列为首要。
  - docs/15：信标数量口径 **74→61**（方案 I = 61+10 备用 = 71；方案 II 风险预案 98+10 = 108）；采集终端 **安卓→iOS**（小程序依赖 iBeacon，iOS 回报 accuracy/txPower，Android 常 null）；新增「精度投入依据」（算法优先于硬件）；B 表三档 × 两方案小计；预算合计拆方案 I/II 两表。
- ⚠️ 修复：`gen_reports_docx.py` 正文里误用了 markdown `**加粗**`（docx 会原样显示星号），已去掉；后续写 docx 正文禁止用 md 语法（md 文件里可以用）。
- 三方文件均已验证：docs/14 章节 2.1~2.7 齐全、docs/15 表格 8 张、无 `**` 残留；预算数字逐项复核正确（3,195+973=4,168；4,860+973=5,833 等）。
- 状态：**仅落盘未提交**（铁律0 禁止 master 直接提交），待用户确认后走分支合入。
