

## 人工部署信标模式调整：全局→测试路线（08-15 08:55-09:10）
- 用户需求：手动部署蓝牙信标只针对测试路线，不适用全局场景。原实现（30c4e19 前）把 ble_deployment.json 绑定全局模式（beacon_floors），与需求相反。
- 调整（commit 30c4e19，+69/-43）：①Python 侧 ble_deployment.json 存在时替换 beacon_floors_routes（测试路线），全局不受影响 ②前端 deployCollectBeacons/deployFloorGroup/deployCreateBeaconEl/deployExport 全部 data-mode="global"→"route" ③deployToggleMode 进入部署模式自动 setMode('route')（route 组默认隐藏，保证可见）④文案改「测试路线」语义。
- QA 验证：静态审查+行为复算全 PASS（有 ble→route=3 人工信标/global=5 不受影响；无 ble→route=60；--no-ble-deploy 回退）。QA 报 golden 48/50（2 pipeline 用例崩溃）但主理人亲自复跑 50/50 OK（506.1s），且解析模块两提交 diff=0 → QA 为环境瞬态误报，以主理人复跑为准。
- 完整 pipeline 重跑：gen_trilateration_plan_routes（60 信标，与基线一致）→ render_interactive → result/floor_layout_v9_interactive.html 已更新（route 60 信标/global 0/BK-M-* 0，部署脚本 route 语义齐全）。
- 分支 feature-manual-beacon-deploy 现含 3 commit：49f2f9c（功能）+7e12c10（点选修复）+30c4e19（模式调整）。待用户确认合入 master。
- 用户已在浏览器导出并保存 result/ble_deployment.json（66 个信标：F1 59 + F2 7，schemaVersion 1.0，人工部署导出），要求重新生成渲染。
- 重新运行 render_interactive.py（`python -m src.rendering.render_interactive`，需在仓库根目录，PYTHONPATH 方式在 Git Bash 下不生效）：日志确认「加载人工部署方案 ble_deployment.json（测试路线）: 66 个信标」，生成 result/floor_layout_v9_interactive.html。
- 验证：HTML 中 class="layer_beacon" 66 个与部署文件一致；data-info 含 beaconId BK-01-xxx；deploy-bar/deployToggleMode 部署入口存在；route 组 820 元素（指纹点+覆盖点+信标点）。已呈现给用户。
- 评估人工部署方案（ble_deployment.json 66 信标）在测试路线的三点定位质量（debug/eval_ble_deployment.py 复用 FloorModel 口径）。
- 结果：覆盖 97.48%（734/753）为三方案最高（基线60: 92.17% / 优化90: 94.55%）；退化点 239（GDOP>3 31 / 外扩 208）少于基线 398 与优化 262。
- 问题：F1 墙角违 20（33.9%）+ 柱违 8（13.56%）放置质量差于基线（15/1）；F2 覆盖仅 86.11%（5 缺口点）；F1 GDOP>3 退化 31 略高于基线 16。
- 改进建议：①21 个放置违规信标微调 0.5-1m ②F2 补 2-3 信标达 95%+ ③F1 (-33~-37, 17~20) 外扩退化走廊区对侧补 1 信标。
- 完整报告 result/beacon_deployment_evaluation.json；评估脚本 debug/eval_ble_deployment.py（可复用）。
- 新增「部署模式删除选中信标」功能（feature-manual-beacon-deploy commit 5f5f39d）：
  * deploySelectEntry/deployClearSelection/deploySelectByEl 选中语义（点击信标橙色高亮，mousedown 统一清旧选中）
  * deployDeleteSelected：confirm 防误删 → DEPLOY_BEACONS splice + DOM removeChild + 清选中；导出自然反映删除
  * keydown Delete/Backspace 删除（INPUT/TEXTAREA/SELECT 聚焦不响应）；新增信标后立即选中
  * 模板 deploy-bar 加「删除选中信标」按钮（初始 disabled，选中启用）；退出部署模式清空选中
  * 验证：HTML 66 信标加载 + 语义齐全 + node --check JS 语法 OK；HTML diff 仅部署脚本区 + beacon-contrib-data（66 vs 60 信标贡献差异，源自 ble_deployment.json 重渲染）
  * 已 push 远端 feature-manual-beacon-deploy（4 commits：49f2f9c+7e12c10+30c4e19+5f5f39d），待用户确认合入 master
- 用户更新 ble_deployment.json（22:24 导出，66 信标：base 48/fill 12/manual 6，F1 59 + F2 7）→ 重新渲染 + 评估。
- 对比上次：覆盖保持 97.48% 最优；退化 239→231（外扩 208→190 改善 18，GDOP>3 31→41 恶化 10）；NN-CV 0.455→0.371（更均匀）；放置违规增多（墙角 24→27、柱 8→12，共 29 处涉及）。
- ⚠️ 新出现严重共线退化：(-41.06, 19~22) 三点 GDOP 14.8~24.3（上次最差仅 1.27）——BK-01-002 移入该走廊同侧后 3 最近信标近共线，需对侧补/移 1 信标破共线。
- F2 仍 86.11%（7 信标，外扩 1.1~2.4m）；最差外扩点 F1 (-35.06,19.36) 5.91m、(-19.07,-76.64) 5.72m。
- 完整报告 result/beacon_deployment_evaluation.json 已更新。
- 测试路线起点统一改为 F1-RM-0016（音乐教室）→ beacon_routes.json 两条路线 start 改为其 TR 节点 F1-TR-0016（路由图节点 ID 为 F1-TR-0016，F1-RM-0016 不在拓扑节点中，shortest_path 要求节点 ID）。
- 指纹网格 route 重生成：753 → 856 点（F1 610 + F2 246，F2 从 36 扩 7 倍——路线2 跨层后 F2 路径覆盖西侧走廊(-37~-48)+中部连接走廊(y-20~-58)+东侧）。
- 基线信标方案重生成：67（F1 53 + F2 14）。
- 用户 ble_deployment.json 已更新为 61 信标（F1 52 + F2 9，22:38 导出，用删除功能删 5 个）。
- 评估：F1 人工 97.54% 优秀（退化 132 vs 基线 350）；⚠️ F2 人工仅 31.3%（246 点中 169 缺口，143 点 0 可见）——9 个 F2 信标全集中在东侧(-34~-28, -82~-70)，西侧/中部连接走廊无信标。基线算法 F2 也仅 51.63%。
- 补点建议：F2 需补 ~15-18 信标（西侧电梯口 -37,-9 一带 + 西侧走廊 -44,-5 + 中部连接走廊 -48,-20 → -35,-58 沿线）。
- 评估口径改为「视障模式」：beacon_routes.json mode normal→blind（label 加「视障」前缀）。
- 盲模式指纹网格重生成：856 → 647 点（F1 611 + F2 36——盲模式剔除不可达走廊，F2 从 246 缩回 36，路线2 节点 19→22）；基线方案 67→60（F1 53 + F2 7）。
- 视障模式评估（人工 61 信标）：总体覆盖 97.69%（F1 97.55% + F2 100%），退化 138（GDOP>3 26 / 外扩 112）为三方案最少（基线 356/优化 208）。61 信标以 0.16pp 覆盖差（vs 优化 90）获得最好定位质量，性价比最高。
- 残留：F1 退化 132（外扩 106 为主，走廊固有）+ 放置违规（F1 墙角17 柱11 / F2 墙角5）。
- 注意：normal 路线下 F2 31.3% 覆盖不足的问题在视障模式不存在（盲模式 F2 仅 36 可达点）。

## 合入 master 执行记录（2026-08-15 22:54）
- 用户确认合入 feature-manual-beacon-deploy（8 commits：49f2f9c+7e12c10+30c4e19+5f5f39d+0634a6a+6e330d6+ca877c7+3e06bbc），**连同 result/ble_deployment.json 入库**（.gitignore 加 `!result/ble_deployment.json` 例外，commit 3e06bbc）。
- 合入方式：safe-merge 脚本两次因 `.gitignore` unlink Invalid argument（Windows 文件锁）中断，且触发目录误删 bug（checkout 后大量文件报 D，`git checkout -- .` 从 index 恢复）。**最终解法：先 `git add .gitignore`（index 与目标 commit 的 .gitignore blob 一致）再 `git reset --hard 3e06bbc`——git 对内容一致的文件跳过 unlink 写盘，快进成功。**
- master: d03bad4 → 3e06bbc（ff），push 成功；本地+远端 feature 分支已删；暂存产物已恢复。
- ⚠️ 经验：Windows 上 .gitignore 被锁（unlink 失败）时，reset --hard/merge 会 abort；先让 index/工作树该文件与目标一致再 reset 可绕过。
