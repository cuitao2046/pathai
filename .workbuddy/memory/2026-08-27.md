# 2026-08-27 工作日志

## 1. 分析 ble_deployment.json 结构与文档现状
- `result/ble_deployment.json` = 信标部署规划方案（98 枚，P0 后），非地图几何文件；地图靠 `sourceNodeId`/`adjacentRooms` ID 引用 + `locationDesc` 描述关联 geojson。
- "不同层"两层含义：① `floor` 物理楼层(1/2)；② `semanticTag`+`subType` 表达 L2 定位层布点角色(base/fill/dir/manual_deploy/elevator_door)，非 L5 应用语义。
- **无专门字段级 schema 文档**：相关说明散落 docs/17(语义审计)、docs/18(TODO 增补 adjacentRooms)、docs/16(复用资产)、各工具 docstring。
- 发现数据质量问题：`summary.total=61` 与正文 `beacons` 实际 98 不一致（P0 扩容未同步 summary）。

## 2. 回滚 ble_deployment.json 至 61 信标版本（用户要求）
- 密度修复(扩容 61→98)发生在 `44c2658`(P0)。其父 `88c3549` 即 61 枚 + summary 同步版本。
- 新建分支 `fix-rollback-ble-61`（基于最新 master `6492e95`），`git checkout 88c3549 -- result/ble_deployment.json`，提交 `533a006`（1 file changed, 1004 deletions），已 `git push -u origin fix-rollback-ble-61`。
- 仅改动 `result/ble_deployment.json` 单文件，未触碰源码。
- **待用户确认后 FF 合入 master + 删除分支**（铁律0）。

## 3. ⚠️ git 误删父目录 bug 再次触发（重要）
- 切回 `feat-fp-collector` 时，`.workbuddy/memory/` 整目录被误删：15 个 tracked 日志(2026-08-04~20 + MEMORY.md) + 1 个 untracked 的 `2026-08-27.md`。
- tracked 部分用 `git checkout HEAD -- .workbuddy/memory/` 恢复（18 文件正常）。
- **`2026-08-27.md` 为 untracked，不在对象库，本次永久丢失**（含早先 MinewBeaconAdmin 目录分析记录，已从对话重建本文件）。
- **新结论**：该 bug 不止删 tracked，会连目录内 untracked 文件一并删除且不可恢复。切分支前必须先 `git add`+`commit` 所有 memory 日志，或 `git stash -u` 保护 untracked。已更新 MEMORY.md 对应条目。

## 4. MinewBeaconAdmin 目录分析（早先，本次重建记录）
- 内容：Readme.md、iOS(MinewBeaconManager/Beacon/Connection/Setting.h framework)、Android(aar)、`!!!【重要】`真机用 ARM 包、GATT 写配置(exportJSON/importJSON)、密码 8 位硬约束。
- 结论：信标刷机/批量配置工具，与指纹采集小程序(只读 iBeacon 扫描)互补不可互替。
