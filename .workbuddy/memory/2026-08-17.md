# 2026-08-17 工作日志

## 未提交内容通过新分支合入 master（feat-fingerprint-coverage-checker）
- 背景：8/15~8/16 指纹覆盖校验工作遗留的未提交本地改动（memory 笔记 + coverage_checker.py）尚未入库；远端 master 已前进到 20f61a9（含指纹有效区并集提取 6c973e6 + 三方案覆盖/精度校验器 20f61a9 两个 commit）。
- 流程（铁律0）：基于 master(20f61a9) 切分支 `feat-fingerprint-coverage-checker` → 精确 add（3 个 memory 文件 + debug/coverage_checker.py，排除 3 个 ble_refine 调试产物）→ commit → push → 人工校验 → master FF 合入 → push master → 删分支（本地+远端）。
- 入库范围：
  - `.workbuddy/memory/2026-08-14.md`（+6：指纹有效区间 SVG→并集 JSON 提取）
  - `.workbuddy/memory/2026-08-15.md`（+7：指纹区 JSON 入库 + 两点法校验器记录）
  - `.workbuddy/memory/2026-08-16.md`（新增日志）
  - `debug/coverage_checker.py`（两点法覆盖/精度校验器：有效区撒 0.6m 网格→LOS 过滤→最大张角锚点对→解析误差 σ_pos=σ_d·√(1+|cosφ|)/sinφ）
- 排除（临时调试产物，按 .gitignore 约定不入库）：`.ble_refine_corridor_walls.json` / `.ble_refine_parse.json` / `ble_refine_corridor_report.md`
- 关键发现记录：两点法仅覆盖有效区 ~27%（走廊平行墙致张角退化≈0°/180°），606+254 歧义点；建议走廊改直角坐标法或 3+ 锚点三边测量。
- 结论：本地与远端 master 重新同步，无 commit 差异残留。
