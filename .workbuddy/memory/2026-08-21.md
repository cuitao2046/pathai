# 2026-08-21 工作日志

## 测试路线信标部署密度复核（review，未改码）
- 对象：当前 `result/ble_deployment.json`（pathai-manual-deploy, 61 信标：F1 52 / F2 9；semantic: trilateration_route_base 43 + fill 10 + manual_deploy 8）。
- 对照 docs/13 优化目标：74 信标（F1 67 / F2 7）；路线掩码 6m。
- 实测复核（647 路线指纹点 vs 61 信标，KDTree/几何 GDOP）：
  - **覆盖（≥3 信标）**：F1 R=5m 仅 52.5%、R=7m 98.9%、R=10/11m 100%；F2 R=5m 97.2%、R=7m+ 100%。
  - **几何 GDOP**：F1 中位 1.28、最大 12.0，GDOP≤2.0(3m@1.5m) 达标 91.5%，GDOP>3 退化 4.9%(30/611)；F2 中位 1.33、最大 2.98、达标 91.7%、退化 0%。整体与 docs/13 的 90.2% 一致。
  - **路线贴合(规则[7])**：0/61 信标距路线>6m（F1 最大 3.5m、F2 1.9m）→ 无路线外违规，比 74 方案干净。
  - **间距**：F1 NN 中位 3.59m/最小 0.70m(BK-01-032↔033 冗余+互扰)/最大 6.92m；F2 NN 中位 2.38m。
- **结论**：名义 BLE 范围(7–11m)下密度合理且≥docs/13 目标；**脆弱点**：①真实走廊人体遮挡/多径使有效范围常降至 ~5m，此时 F1 覆盖崩到 52.5% 出现盲区；②F1 30 个 GDOP>3 退化点(最差 12.0) 多位于单侧布信标走廊(坐标 y≈-13~-72 段)；③BK-01-032/033 0.7m 过密对需剔除/重位。
- **建议（待用户拍板是否开分支）**：①F1 稀疏段(最大 NN 6.92m 处)按 5m 有效范围补点使 ≥3@5m 全路由覆盖；②为 30 个退化点就近加 1 枚跨走廊信标或确认指纹/中轴约束兜底；③剔除 0.7m 过密对；④F2 无需动。
- 复核脚本已删，方法可复现（beacons vs fingerprint_grid_routes 点云，GDOP=sqrt(trace((GᵀG)⁻¹))）。

## P0 信标密度修复（开分支 fix-p0-beacon-density）
- 用户拍板：**只落两个 P0**（F1 5m 全覆盖 + 30 个 GDOP>3 退化点降 GDOP），**不碰其他**（F2 不动、过密对剔除 P1 不动、adjacentRooms/R6/渲染等无关模块不动）。
- 新增 `src/tools/improve_p0_coverage.py`：F1 路线点贪心补点（覆盖 pass：最缺优先，沿走廊中轴优先+墙偏移兜底，MIN_SPACING=2.5m 防过密；GDOP pass：对退化点沿法向跨走廊偏移破共线）。幂等（同输入重跑新增 0）。
- 结果（权威前后对比，F1）：
  - **@5m ≥3 信标覆盖**：52.5% → **100%**（611/611，0 未覆盖）。
  - **GDOP>3 退化点**：30 → **1**（最差 12.0→6.9，降幅 96.7%）。
  - 新增 37 枚 F1 信标（BK-TR-F1-009 续编，minor 20003 续编，schema 镜像既有 route_fill；sourceNodeType=route_fill 不触发 R6）。
  - 残余 1 个 GDOP>3 点：3 最近信标 0.35/2.23/3.23m，0.35m 处已有信标，受 2.5m 间距约束无法就近破共线 → 结构性硬残余，不为它破坏间距规则（属 P1 范畴，按用户指示不动）。
- 同步：`inject_adjacent_rooms.py` 重跑（98/98 含 adjacentRooms，R6 ERROR=0）；重渲染 HTML；`validate_beacon_deployment` R1/R5/R6 ERROR=0（R4 WARN 13→21 增量为新增靠近楼梯填充信标，非楼梯地标、预期、不阻断）。
- ⚠️ **途中触发 git 误删父目录 bug**：`git checkout`/运行期间 src/* 与 .workbuddy/* 共 60+ 跟踪文件从工作树消失（HEAD 完好）。已 `cp result/ble_deployment.json /tmp/ble_deployment_p0_backup.json` 备份后 `git checkout HEAD -- src/ .workbuddy/` 恢复；ble_deployment.json 仍为 89 信标版本、未丢失。教训：渲染/测试前先 `git status -s | grep '^ D'` 自检。
- golden 回归 **50 tests OK**（恢复后重跑）。合入 master 后 golden 全绿。
