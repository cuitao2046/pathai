# 2026-08-28 工作日志

## 补 ble_deployment.json 字段说明文档（用户要求"补上字段说明文档"）
- 接受上一轮"在合入前一并补上"的提议，在回滚分支 `fix-rollback-ble-61` 上新增
  `docs/19-ble_deployment.json数据结构与字段说明.md`，提交 `920e12e`，已 `git push -u origin fix-rollback-ble-61`（远端 `533a006..920e12e`）。
- 文档内容：文件定位（信标部署规划，靠 `sourceNodeId`/`adjacentRooms` ID 引用地图，不内嵌地图几何）+ 坐标系约定 + 顶层结构 + 单信标 24 字段（类型/必填率/枚举全覆盖）+ 明确区分"物理楼层 floor"与"L2 定位层布点角色 semanticTag/subType" + `summary` 一致性约束 + 与地图关联方式 + 版本变更表（含本次回滚）。
- 字段 schema 基于 61 版本实际提取；必填 18 字段全 61/61，可选字段（installDate 43/61、originalPlannedCoordinates 41/61、direction 13/61、original* 11/61）已标注出现率。

## git 误删 bug 再次触发
- 本次 `git checkout fix-rollback-ble-61` 时 `.workbuddy/memory/` 再次被误删（15 个 tracked 日志），立即 `git checkout HEAD -- .workbuddy/memory/` 恢复，无永久损失。
- 印证：该 bug 在切到 fix 分支时稳定复现；切回 feat 时本次未触发。仍须切分支后立即 `git status` 自检。

## 当前分支状态
- `fix-rollback-ble-61`：含回滚(533a006) + 文档(920e12e)，已推送，**待用户确认后 FF 合入 master + 删分支**。
- `feat-fp-collector`：指纹采集小程序开发中（未合入）；本会话未在其提交文档/回滚，保持纯净。
