# CAD 图纸关键元素识别方案

> 以 `A20-002/003-II-初中学部 1# 教学楼首层/二层平面图`（BIAD 导出 PDF）为例，总结从 CAD 矢量 PDF 中识别**墙体、封闭空间（房间）、门洞、窗、楼梯/电梯/柱**等关键元素并生成楼层 GeoJSON 的完整技术方案。
>
> 实现代码：`src/parse_cad_pdf.py`（解析）、`src/render_map.py`（渲染）、`src/validate_geojson.py`（QA）、`src/topology.py`（第五章导航拓扑建模）。
> 输出：`result/school_building_01_map_v9.geojson`（含 geometry / semantic / topology / accessibility / crossFloorEdges）、`result/map_render_f{1,2}*.png` 及 `*_topology.png`。

---

## 1. 总体流程

```
CAD PDF（OCG 图层）
  │
  ├─ 1. 图层筛选：仅解析"默认开启"的图层（PyMuPDF OCG）
  ├─ 2. 坐标标定：轴网间距 → SCALE=0.0529 m/pt，原点 + Y 翻转
  │
  ├─ 3. 按图层分流
  │     ├─ 结构层（WALL/STAIR/EVTR/COLUMN…）→ 墙线段集合
  │     ├─ 家具层（A-METAL-S/A-TECH-SANT）  → 细线参与封闭
  │     ├─ window 层                        → 分类：窗口线 / 门扇摆弧 / 编号笔画(剔除)
  │     ├─ DOOR_FIRE 层                     → 防火门摆弧
  │     └─ 文字（非 OCG 墨线外的文本）        → 房间标签点
  │
  ├─ 4. 墙体矢量化：端点量化吸附 → 共线合并(桥接 ≤30pt 缝隙)
  ├─ 5. 房间识别（栅格管线）：栅格化 → 连通域 → 分水岭归属
  │        → 标签探测 → 守卫式泛洪合并 → 轮廓多边形化
  ├─ 6. 门洞识别：摆弧几何（铰链/闭门端/弧中点）→ 聚类去重（一洞一门）
  ├─ 7. 门归属：5 段链（弧中点包含 → 侧向投票 → 距离兜底 → 零门认领 → 偷取）
  └─ 8. 组装 GeoJSON（geometry/semantic/topology/accessibility）→ QA → 渲染
```

---

## 2. 图层策略：只扫描"默认开启"的图层

CAD 导出的 PDF 用 OCG（Optional Content Group）组织图层，且很多图层默认关闭（如家具、标注、电气等）。**只解析默认开启的图层**，与人工看图所见一致：

```python
def get_default_on_layers(doc):
    return [name for name, on in doc.layer_ui_configs() if on]  # 伪代码
```

**关键坑：PDF 页面旋转**。本套图纸 `page.rotation = 270`，而 `get_drawings()` / `get_text()` 返回的是**未旋转**的原始坐标。做对照渲染时必须 `page.set_rotation(0)`，否则坐标系对不上。

图层按语义分四类处理：

| 类别 | 图层 | 用途 |
|---|---|---|
| 结构层 | `WALL`、`A-FLOR-STRS`、`STAIR`、`A-FLOR-EVTR`、`COLUMN`、`柱子-刚结构` | 房间轮廓封闭的主力墙体 |
| 家具层 | `A-METAL-S`、`A-TECH-SANT` | 含卫生间真实隔墙（缺了房间不闭合），也含厕位隔断/洗手台（会切碎房间）——需特殊处理（§5.4） |
| 门窗层 | `window`、`DOOR_FIRE` | 窗口线段 + 门扇摆弧 + 门窗编号笔画（矢量化 glyph，需剔除） |
| 标注层（排除） | `AXIS`、`A-ANNO-*` | 轴线/文字/标高/图签，不参与封闭，避免切分房间 |

> **教训**：该 CAD 导出的墙体分散在多个结构图层中，必须取**并集**才能闭合房间轮廓；只取 `WALL` 层会漏掉大量墙体段。

---

## 3. 坐标系标定

- **比例尺**：轴网（AXIS 层）间距众数 158.8pt = 8400mm → `SCALE = 0.0529 m/pt`；用窗编号 M2GW5924（5900mm = 111.5pt）交叉验证。v7 旧值 0.0644 偏大 22%，已弃用。
- **原点与翻转**：`ORIGIN = (2019.1, 1154.8) pt`（与 v7 对齐），PDF y 轴向下、地图 y 轴向北向上，故：
  ```
  xm = (xpt - ORIGIN_X) * SCALE
  ym = (ORIGIN_Y - ypt) * SCALE      # Y 翻转
  ```

---

## 4. 墙体矢量化

1. **端点量化吸附**（`GRID_QUANT = 1.0pt`）：消除 CAD 线端点的微小间隙。
2. **共线合并** `merge_collinear(angle_tol=2°, axis_tol=2pt, gap_tol=30pt)`：把同一直线上、间距 ≤30pt 的墙段桥接为整段。
   - 依据：门洞缝隙在 gap 直方图上呈 **20~22pt 尖峰**（≈1.1m 宽门，两侧为长墙段）；厕位门缝隙 8~14pt（两侧为短段）。
   - **失败教训**：曾尝试"两端皆短段则不桥接"规则重开厕位门缝，但**虚线墙也由短段+大间隙构成**，规则导致全图墙体碎裂（F1 房间 30→17）。最终回退为无条件 30pt 桥接——门洞由专门的"封口线"机制处理（§6），不在墙体合并阶段区分。
3. **家具层线段**：以细线（1px）单独栅格化参与封闭，但在微单元邻域内擦除（见 §5.4），实现"既封闭真实隔墙、又打通厕位格"。

---

## 5. 封闭空间（房间）识别：栅格管线

纯矢量求交对 CAD 断线/虚线间隙极其脆弱，因此采用**栅格化 + 形态学**方案（OpenCV，`RENDER_ZOOM = 3.0 px/pt`）。全程只用 OCG 矢量线，不引入文字/标注墨线。

### 5.1 栅格化 `rasterize_walls`

- 全部墙段（结构+家具）以 **2px** 绘制，且每段**端点外延 6pt**（`WALL_EXT_PT`）——桥接 T 型接头/窗洞收口处的 2~6pt 缝隙；
- 门/窗洞口**封口线**以 3px 绘制（把门洞先"封死"，让房间闭合，门洞位置由门识别阶段独立记录）；
- 形态学密封三连：
  - 9×9 椭圆闭运算（桥接转角/T 型接头细缝，9px ≈ 3pt，远小于 ≥14pt 的门洞，不会误封门洞）；
  - 17×1 / 1×17 方向性闭运算（桥接轴对齐墙线端部缝隙 ≈5.7pt，对 ≥30px 的洞口无影响）。
- **失败教训**：本图真实墙体也是 **2px 单线**（调试图中看到的"粗墙"全是闭运算产物），因此"开运算去薄隔墙"会把真墙一起溶掉，不可用；也不能用像素厚度区分"真墙 vs 厕位隔断"（隔断双线对栅格化后同样 8~10px 厚）。

### 5.2 连通域与室外剔除

- 墙图取反 → `connectedComponentsWithStats`（4 连通）提自由空间；
- 面积 ≥ `ABSORB_CELL_M2 = 2m²` 的为"区域"（房间候选）；< 2m² 的为"微单元"（厕位格/器具格）；
- **接触图像边界的连通域 = 室外**，单独标记。

### 5.3 分水岭归属（owner grid）

微单元（厕位格）需要归属于某个房间，但简单的"膨胀合并"会被室外抢先：

- 归属图 `owner`：墙 px = `-1e9`（永不扩散）；区域 px = 正 cid；室外 px = `-1`；微单元 px = `0`（可填充）；
- **多源生长 80 轮**（80px ≈ 1.4m，足以穿越厕位/管井进深）：每轮已占领 px 向 4 邻域的可填充 px 扩展，取**邻域最大值**——房间正 id 恒胜过室外的 `-1`，墙 `-1e9` 恒败；
- 结果：每个微单元归入"最先到达"的房间，厕所隔断格自然并入卫生间而不是走廊。

### 5.4 标签探测与种子

房间名称来自 PDF 文本（字号 ≥ `LABEL_MIN_SIZE = 9.5pt`，剔除图签区 x > 2900pt）：

1. **探测**：在 owner 归属图上直查标签点归属（标签落在微单元里也能查到所属房间）；失败则以 6→90px 递增半径做**众数投票**（取邻域中出现最多的房间 id，防止单点漂移误抢——如"音乐教室"标签抢走隔壁"乐器存放室"的区域）；
2. **外观标签黑名单** `LABEL_SKIP_RE`：`非机动车车库入口|非机动车车库出入口|消防车道|车道|雨棚|屋面|上空|庭园|台阶|坡道|散水|屋顶平台|泄爆井|不上人屋面`——**仅**排除室外/开敞/图签类标签；注意 `出入口` **不再**进黑名单，以便 `无障碍出入口`/`门厅`/`人防主出入口` 等被正确识别为建筑出入口（见 §6.5）。`LABEL_MIN_SIZE` 由 11.5pt 下调到 **9.5pt**，以捕获约 10~11pt 的窄长标签（如"门厅无障碍出入口""人防主出入口"）；
3. **标签播种**：当标签落在被切碎的小 cell 中（90px 内无 ≥2m² 区域），直接把该 cell（0.3~9m²）认领为房间种子；
4. **图签区伪标签剔除**：`is_room_label` 显式排除归档记录 / 出图日期 / 图号 / 会签 / 验证签字 / 教育建筑 / "门窗洞口" / "范围内" / "年 月 日" 等图签与说明性文字，避免把标题栏/说明误判为房间（如伪标签"2m 范围内无门窗洞口"曾被判为房间）。

### 5.5 守卫式泛洪合并

仍有一些未标注小区域（[2, 9) m²，如被隔断围合的厕位排、凹位、门斗）需要并入邻房：

- 规则：某未标注 cell 在 **12px 半径**内**恰好只有一个**已标注房间邻居时，并入该房间；按面积降序处理、链式 4 轮；
- 12px 的设计意图：能跨越 ~10px 的栅格化厕位隔断双线对，但跨不过 16~20px 的双面真墙；"唯一邻居"约束防止跨房间误并（因此**不需要**"附近不能有室外"的边界守卫——该守卫曾把门洞封口旁、最需要合并的入口 cell 全部挡住，已移除）；
- 家具线的妙用：凡围出 <2m² 微单元的家具线在微单元邻域内擦除（打通厕位与走道）；真实隔墙两侧都是大房间、邻域无微单元，不受影响。

### 5.6 多边形化

每个已归属房间：room_mask 上先做 9×9 闭运算（桥接室内隔断缝隙）→ `findContours` → `approxPolyDP` → shapely Polygon。面积约束 `MIN/MAX_ROOM_AREA_M2 = 3~600m²`。

---

## 6. 门洞识别（`window` 层分类 + 摆弧几何）

### 6.1 window 图层分类 `classify_window_layer`

window 层混有三类元素，需先分流：

| 元素 | 判定 | 去向 |
|---|---|---|
| 门窗**编号笔画** | 长度 < `TINY_STROKE = 8pt`（≈0.5m）且端点不能吸附到任何长线 | 剔除（编号是矢量曲线，不是文本） |
| **窗口线** | 长线按"平行(<5°) + 垂直间距 ≤6pt + 轴向投影重叠"分组 | windowSegments（组轴向包络 = 窗口轴线） |
| **门扇摆弧** | bezier 曲线 | 交门洞识别 |

### 6.2 摆弧 → 门洞 `detect_doors`

铰链门在 CAD 中是**四分之一圆贝塞尔摆弧**：

- **铰链（圆心）**：弧 bbox 四角点中**离弧中点（bezier 中点）最远**的角点——弧背向圆心鼓出；
- **闭门端**：弧两端点中**离墙线最近**者（另一端是伸入房间的门板端）；
- **门洞线段 = 铰链 → 闭门端**，门宽 = 弧半径；
- **弧中点 `arc_mid`**：标记摆动侧——门向内开时弧必然鼓入所服务房间（最重要的归属信号，但有例外，见 §7）；
- 半径 < `MIN_DOOR_WIDTH_PT = 9pt`（≈0.48m）的弧为厕位/器具小弧，丢弃；
- `DOOR_FIRE` 层的摆弧同样处理，标记 `doorType = fire`。

### 6.3 一洞一门（去重）

同一门洞可能被多条弧/线表达（双扇门、门套线等）。门洞线段按"**平行(<30°) + 垂直间距 ≤6pt + 轴向间隙 <6pt**"聚类，**每个聚类只保留门洞线段最宽的一扇**（`mergedCount` 记录合并数）——满足"每一个门洞只需要一扇门"。

---

## 6.4 无摆弧门洞（DK 洞口 / 通道）识别

§6.2 的摆弧法只覆盖有四分之一圆贝塞尔的门扇。**楼梯间门洞、建筑出入口、大厅/图书馆等公共空间的门洞在 `window` 与 `DOOR_FIRE` 层往往没有摆弧**——例如楼梯间门洞的 `window` 层只有 `DK` 开头的洞口编号矢量笔画（DK2424 等），没有门扇弧。这类门洞必须换一套判据。

### 6.4.1 关键发现：DK 编号是矢量笔画而非文本

`DK` 洞口编号**不在 PDF 文本层**（文本层 DK 计数 = 0），而是 `window` 层的矢量笔画。F1 的 `window` 层有 **14585 条短笔画（<8pt）**，经 `cluster_window_glyph_codes(link=6pt, min_strokes=8)` 聚成 **157 个字形块（glyph block）**。因此"window 层是否含 DK 开头矢量文字"可作为门洞判据之一，但单靠字形块召回不全，**几何判据更稳健**。

### 6.4.2 几何判据：墙缝法（wall-seam openings）

`merge_collinear` 在桥接 ≤30pt 缝隙时会**记录被桥接的 gap 位置**（"wall_gaps"）——这些缺口正是墙上的门洞。筛选条件：

- 缺口两侧墙段都较长（≥ `OPENING_FLANK_MIN_PT = 18pt`），说明是真正的门洞而非碎缝；
- 缺口宽度落在 [`MIN_OPENING_WIDTH_PT = 12pt`, `MAX_OPENING_WIDTH_PT = 80pt`]（≈0.63~4.2m）区间；
- 该缺口**未被已知摆弧门（swing/fire）或窗**覆盖；
- 且缺口中心 `OPENING_CODE_NEAR_PT = 30pt` 内**存在 window 层字形块**（DK 编号或门编号笔画），作为"此处确实是洞口"的确认。

满足上述全部条件的墙缝记为 `kind="opening"` 门洞：以几何中心为门心（无 `arc_mid` 摆向，沿用 §7 的 5 段链兜底归属），`sourceLayer="window+geometry"`。

> **成果**：F1 识别 7 个、F2 识别 5 个无摆弧门洞（楼梯间门、建筑出入口、公共空间通道等），与摆弧门一并纳入 §7 归属与 §9.4 拓扑。

---

## 6.5 出入口与公共空间识别（语义分类）

### 6.5.1 元素分布

建筑出入口、大厅、图书馆等公共空间门洞的元素分布在 **`window` 与 `DOOR_FIRE` 层**：摆弧门来自这两层（§6.2），无摆弧门来自 §6.4 的墙缝法；电梯间由 `A-FLOR-EVTR` 几何识别，楼梯间由 `STAIR` + `A-FLOR-STRS` 合并识别（§8）。

### 6.5.2 语义分类 `ROOM_TYPE_RULES` 扩充

房间类型由关键词表归类，本次扩充：

| 标签关键词 | roomType | 说明 |
|---|---|---|
| `门厅无障碍出入口` / `无障碍出入口` | `accessible_entrance` | 无障碍出入口 |
| `人防主出入口` / `出入口` | `entrance` | 普通建筑出入口 |
| `大厅` / `门厅` | `lobby` | 门厅/大厅 |
| `楼梯间`（含 `无障碍楼梯`） | `staircase` | 普通/无障碍楼梯间（标签含"无障碍"则 `accessible` 标 true） |
| `电梯` / `电梯间` | 由 `A-FLOR-EVTR` 几何识别 | `elevator` |
| `合班教室` / `图书资料室` / `阅览` / `活动空间` / `报告厅` | `classroom` / `library` / `activity` | 各类公共空间 |
| `过道` / `走道` | `corridor` | 走廊 |

派生集合：`ROOM_PUBLIC_TYPES`（公共空间）、`ACCESSIBLE_TYPES`（无障碍可达）、`NON_ACCESSIBLE_TYPES={"staircase","shaft"}`（视障禁用）、`INDEPENDENT_ENTRANCE_TYPES`。

### 6.5.3 独立出入口节点

凡是语义为出入口但**未匹配到房间多边形**的标签（门厅、人防主出入口、无障碍出入口等），不作为房间处理，而是直接作为 `facility_entrance` 拓扑节点接入（见 §9.4），保证建筑出入口在导航图中可被寻路到达。F1 生成 15 个此类节点。

> **关键坑**：`LABEL_SKIP_RE` 的黑名单**不能**包含 `出入口`，否则 `人防主出入口`/`无障碍出入口`/`门厅` 会被排除、无法识别；同时 `LABEL_MIN_SIZE` 须降到 9.5pt 才能捕获窄长标签（§5.4）。

---

## 7. 门 → 房间归属：5 段链

门识别出来后，必须挂到正确的房间上（核心需求：每个封闭空间都要有门）。单一规则不可靠，采用**按可信度递减的级联**：

| 阶段 | 规则 | 说明 |
|---|---|---|
| **pass 0** | `arc_mid` 落在某房间多边形**内部** → 归该房间（多包含时取弧中点最深入者；无包含时 8pt 内取最近） | 门向内开，弧鼓入所服务房间——最可靠信号 |
| **pass 1** | **两侧投票**：门轴法线 ±6pt 各取一点，在 owner 归属图中找 45px（15pt）内最近的已标注房间像素 | 门天然属于两侧空间（房间+走廊或两房间），与邻房角点距离无关 |
| 兜底 1 | 门中心距房间边界 < 4pt（**仅补空**，不与投票冲突） | 会把邻房角点误挂，故只在门无任何归属时启用 |
| 兜底 2 | 门中心 12pt 内取最近房间边界 | 纯距离兜底 |
| 兜底 3 | **零门房间认领孤儿门**：无门房间认领 30pt 内最近的未归属门，要求次近候选 > 1.5× 距离（唯一性），每房限 1 扇 | 卫生间/储藏等多边形被隔断切碎、未能延伸到门所在墙体 |
| 兜底 4 | **零门房间"偷取"多门房间的门**：从仍保有 ≥2 扇门的房间处偷取 30pt 内最近的门 | 典型案：MGD1124 与乐器存放室之间隔着 DK2424 高窗洞口带（21.5pt，被音乐教室争抢、泛洪无法合并），门先被 4pt 规则挂到邻房角点；零门的乐器存放室再把它偷回 |

> **关键教训**：`arc_mid` 摆动方向并非万能——**存在外开门**（MGD1124 向走廊外开），此时弧鼓入走廊而非房间，pass 0 失效，必须靠侧向投票与兜底链补救。无摆弧的 `opening` 门洞没有 `arc_mid`，直接以几何中心进入 pass 1 侧向投票 + 兜底链，效果与摆弧门一致。
>
> **成果（v9）**：F1 30 房间 / 83 门（swing 42、fire 34、opening 7）、F2 19 房间 / 62 门（swing 34、fire 23、opening 5），两层**无门封闭房间均为 0**；F1 16 / F2 6 个未归属门（多为走廊侧 / 未匹配标签空间，见 §11）。

---

## 8. 楼梯 / 电梯 / 柱

- **柱**：`COLUMN`、`柱子-刚结构` 图层的小矩形/填充多边形，`bbox_clusters(gap_pt)` 聚类后直接输出 Polygon；
- **楼梯**：`STAIR` + `A-FLOR-STRS` 图层踏步线组按 bbox 合并聚类（F2 的 `STAIR` 层仅 2 个 quad，必须并入 `A-FLOR-STRS` 才能得到完整楼梯间），外接包络输出 Polygon + 标签（`楼梯{F}F-n`）；标签含"无障碍"则为无障碍楼梯（`accessible=true`，但仍 `NON_ACCESSIBLE_TYPES` 成员，视障禁用）；
- **电梯**：`A-FLOR-EVTR` 图层轿厢轮廓，同理输出 Polygon + 标签（`accessible=true`）；
- 三者同时跨层登记到 `accessibility`（电梯无障碍节点、楼梯风险节点）与 `crossFloorEdges`（1F↔2F 按图纸**井道编号**配对：从 PDF 文本抽取 `FACILITY_CODE_RE`（`II-xx#ST` / `II-xx#EL`）作为权威身份，几何中心距离 <3.5m 仅作无编号兜底，见 §9.4）。

---

## 9. 输出、渲染与 QA

### 9.1 GeoJSON（对齐 v7 schema）

```
venueId / venueName / version / coordinateSystem=local_meters / scale / origin
floors: { "1": { geometry, semantic, topology, accessibility }, "2": {...} }
crossFloorEdges: [...]
```

- `geometry`：walls（LineString）、rooms（Polygon + roomType/label/centroid）、doors（Point + doorType/width_m/rooms[]）、stairs、elevators、columns、windowSegments（+length_m）；
- `semantic.rooms`：id/type/label/centroid/public/accessible（roomType 由 `ROOM_TYPE_RULES` 关键词表归类：卫生间→toilet、教室→classroom、井→shaft …）；
- `topology`：房间节点 ↔ 门节点星形边（含 distance/estimatedTime/无障碍标志）；
- `accessibility`：电梯无障碍节点、楼梯风险节点。

### 9.2 渲染 `render_map.py`

GeoJSON → 每层平面图 PNG：房间按 roomType pastel 配色，标签锚点用 **polylabel**（内切圆心，保证在多边形内且远离边界），字号同时受面积、横向跨度（文本宽）、纵向跨度（字高）三重约束防重叠；墙/窗/平开门蓝圈/防火门红方/柱/楼梯踏步线/电梯叉线 + 指北针/比例尺/右侧图例预留带；中文字体 Microsoft YaHei。`--topology` 额外输出导航拓扑叠加图。

### 9.3 QA `validate_geojson.py`

自动检查：schema 完整性、坐标有限性、门的房间引用存在性、semantic/geometry 一致性、拓扑无悬空边，以及**核心指标——封闭空间（排除走道/楼梯/电梯厅/管井/中庭）无门房间数 = 0**。当前结果 `VALIDATION PASS`。

### 9.4 拓扑图建模（对齐 `docs/03-地图构建指南.md` 第五章）

由独立模块 `src/topology.py` 依据第五章规范生成节点与边，每个楼层 `topology` 含 `nodes` / `edges`，跨层边置于根 `crossFloorEdges`。

**节点类型（5.1）**

| 类型 | 生成规则 | 数量（F1 / F2） |
|---|---|---|
| `room` | 每个封闭房间一个，坐标取质心 | 30 / 19 |
| `doorway` | 每个门洞一个，连接两侧空间；`label` 按 kind 取 门/防火门/通道 | 83 / 62 |
| `intersection` | 每条走廊一个交叉口节点（质心），走廊骨架锚点 | 2 / 2 |
| `facility` | 每个楼梯口（`facilityType=staircase`，盲/轮椅均不可达）/ 电梯口（`elevator`，可达） | 13 / 10 |
| `facility_entrance` | 未匹配房间多边形但语义为出入口的标签（门厅/人防主出入口/无障碍出入口…） | 15 / 0 |

**边属性（5.2）**：`distance`（米）、`estimatedTime`（视障步速 `BLIND_WALK_SPEED=0.8 m/s`）、`accessibilityLevel`（0 平直 / 2 含门槛/坡度 / 999 含楼梯对视障禁用）、`riskLevel`（0.5 普通走廊 / 5 玻璃门 / 10 楼梯口）、`walkable`、`wheelchairAccessible`、`blindAccessible`。

**边构建（同层）**
1. `room ↔ doorway`：房间质心到门心，防火门 `accessibilityLevel=2 / riskLevel=5`；
2. `doorway ↔ intersection`：门心到最近走廊交叉口；
3. `facility ↔ doorway`：楼梯/电梯口连最近门，`staircase` 取 `a=999 / r=10 / 轮椅否 / 盲否`，`elevator` 取 `a=0 / r=1 / 均可达`；
4. `intersection ↔ intersection`：走廊质心互连（≤30m 直连，远距靠房门串接）；
5. `facility_entrance ↔ doorway`：独立出入口连最近门（`r=5`）。

**跨楼层边（5.3，`crossFloorEdges`）**：按图纸井道编号配对（`FACILITY_CODE_RE` 匹配 `II-xx#ST`/`II-xx#EL`，几何中心 <3.5m 仅作无编号兜底），每条边带 `code` 与 `matchedBy: code|geometry`。楼梯 `a=999 / r=10 / 距离=楼层高差 4.2m / 轮椅否 / 盲否`；电梯 `a=0 / r=1 / 均可达`。当前 1F↔2F 共 10 条（楼梯 7 + 电梯 3，全部 `matchedBy:code`）。

> 节点 id 规范 `N{floor}-{kind}{idx}`（如 `N1-ST001` 楼梯接入、`N1-EL003` 电梯接入、`N1-I705` 交叉口、`N1-D208` 门口），边 id `E{floor}-{from}-{to}`，跨层 `CF-{ST/EL}-{idx}`。

---

## 10. 失败实验清单（重要教训）

| 尝试 | 结果 | 结论 |
|---|---|---|
| 家具层细线封闭 + 选择性擦除 | 房间数暴跌（F1=15/F2=5） | 细线封不住真墙、擦除开了室外泄漏口 → 改分水岭归属 |
| 标签探测最近像素（120px） | 垃圾匹配（消防车道抢区域、音乐教室抢乐器存放室的组件） | 改众数投票 ≤90px + 外观标签黑名单 |
| 以"边界是否主要是家具线"判定合并 | 0 次合并 | 厕位隔断在 **WALL 层**（23~27pt 成对线），不在家具层 |
| `merge_collinear` "两端皆短段不桥接" | F1 30→17、F2 17→4 | 虚线墙也是短段+大间隙，误碎真墙 → 无条件桥接 30pt |
| 像素厚度区分真墙/隔断（4px 半径探测） | 0 邻居 | 隔断双线对栅格化后 8~10px，与真墙同厚 |
| 形态学开运算 k=9 "去薄墙" | 0 房间（全溶） | 本图真墙也是 2px 单线 |
| 泛洪合并加"7px 内不能有室外"守卫 | 入口 cell 全被挡 | 门洞封口 3px 之外就是走廊大区域——恰是最需合并的 cell → 删守卫 |
| 12px 半径跨盥洗台合并卫生间整体 | 失败 | 盥洗台台面为 15~34px 实心墨带，合理膨胀无法跨越 → 接受"走道区多边形 + 门侧归属兜底" |
| 仅靠 arc_mid 摆动方向归属 | MGD1124 误归 | 存在外开门，弧鼓入走廊 → 级联归属链 |
| `LABEL_SKIP_RE` 黑名单含 `出入口` | 人防主出入口/无障碍出入口/门厅 全部漏识 | 仅剔除室外/图签类，保留 `出入口` |
| `LABEL_MIN_SIZE=11.5` | "门厅无障碍出入口""人防主出入口"被字号门槛过滤 | 下调到 9.5pt 捕获窄长标签 |
| 楼梯仅取 `STAIR` 图层 | F1 仅 3、F2 仅 1 楼梯间 | 合并 `STAIR`+`A-FLOR-STRS` 线+quad，F1=9/F2=7 |
| 跨层楼梯配对阈值 2.5m | 跨层边=0 | 井道微偏移，放宽到 3.5m → 8 条跨层边 |
| 用 PDF 文本层查 DK 洞口编号 | DK 计数=0，查不到 | DK 是 window 层矢量笔画，改墙缝法+字形块确认 |
| 图签/说明文字当房间 | 归档记录/出图日期/"2m范围内无门窗洞口"被判房间 | `is_room_label` 显式黑名单 + `范围内`/`门窗洞口` 排除 |

---

## 11. 已知限制

1. 少量真实房间标签未匹配到多边形（F1 音乐教室×2、心理辅导室、学生社团活动区、门厅；F2 药品室、教师办公区等），对应门成为孤儿门（F1 16 / F2 6）；
2. 卫生间多边形只覆盖盥洗走道区（门归属已正确，面积偏小）；
3. 拓扑已具备完整节点类型（房间/门口/走廊交叉口/设施/出入口）与属性完整边，并生成 8 条跨层边；但走廊骨架仍为"每走廊 1 个交叉口节点 + 就近连门"的简化模型，未重建 v7 式 700 边中轴路网；
4. 门仅输出 Point + width_m（对齐 v7），未输出铰链/朝向；`opening` 门洞以几何中心替代 `arc_mid`，渲染以符号表达（绿色空心菱形）而非真实门板朝向；
5. 无摆弧门洞依赖"墙缝几何 + window 层字形块确认"，对字形块缺失或被家具线遮挡的洞口可能漏检（F1 7 / F2 5 个已检出的为确信样本）。

---

## 12. 关键参数速查

| 参数 | 值 | 含义 |
|---|---|---|
| `SCALE` | 0.0529 m/pt | 轴网 8400mm=158.8pt 标定 |
| `RENDER_ZOOM` | 3.0 px/pt | 栅格化分辨率 |
| `WALL_EXT_PT` | 6.0 pt | 墙线端点外延（桥接 T 型接头） |
| `ABSORB_CELL_M2` | 2.0 m² | 微单元（厕位格）阈值 |
| `MERGE_REGION_M2` | 9.0 m² | 可泛洪合并的未标注小区域上限 |
| `MIN_DOOR_WIDTH_PT` | 9.0 pt | 房间门最小宽度（≈0.48m） |
| `MIN/MAX_ROOM_AREA_M2` | 3 / 600 m² | 房间面积合法区间 |
| 分水岭生长 | 80 轮 | ≈1.4m，穿越厕位/管井进深 |
| 泛洪合并半径 | 12 px | 跨 10px 隔断对、不跨 16~20px 真墙 |
| `LABEL_MIN_SIZE` | 9.5 pt | 房间标签最小字号（下调以捕获窄长出入口标签） |
| `LABEL_SKIP_RE` | 仅室外/图签类 | 黑名单**不含** `出入口`，否则漏识建筑出入口 |
| `cluster_window_glyph_codes(link=6, min_strokes=8)` | link 6pt / ≥8 笔画 | window 层短笔画聚类成字形块（DK 编号探测） |
| `MIN_OPENING_WIDTH_PT` / `MAX_OPENING_WIDTH_PT` | 12 / 80 pt | 无摆弧门洞（墙缝）宽度合法区间 |
| `OPENING_FLANK_MIN_PT` | 18 pt | 墙缝两侧墙段最小长度（过滤碎缝） |
| `OPENING_CODE_NEAR_PT` | 30 pt | 墙缝中心到 window 字形块的最大确认距离 |
| 跨层配对阈值 | 3.5 m | 同名楼梯/电梯中心距离上限 |
| 视障步速 `BLIND_WALK_SPEED` | 0.8 m/s | `estimatedTime = distance / 0.8` |
| 楼层高差 `floor_height_m` | 4.2 m | 跨层边距离（电梯/楼梯） |
