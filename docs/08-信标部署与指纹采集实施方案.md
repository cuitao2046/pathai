# PathAI 室内导航系统 · 信标部署与指纹采集实施方案

> **面向视障人士的室内导航落地文档。** 本文档基于 `gen_beacon_plan.py --mode route` 和 `generate_fingerprint_grid.py --mode route` 生成的部署方案，详细说明如何在初中 1# 教学楼 1~2 层实施 BLE 信标安装与指纹采集。

---

## 一、总体概述

### 1.1 部署范围

| 项 | 内容 |
|---|---|
| 建筑 | 初中学部 1# 教学楼 |
| 楼层 | 1 层（首层）、2 层 |
| 测试路线 1 | 1F 音乐教室（F1-TR-0015）→ 组织办公（F1-TR-0036），140.4m |
| 测试路线 2 | 跨层：教师办公室（F1-TR-0033）→ 历史教室（F2-TR-0018），146.3m（经电梯） |
| 信标数量 | **48 个**（F1: 44, F2: 4） |
| 指纹采集点 | **753 个**（F1: 717, F2: 36） |

### 1.2 关键指标

| 指标 | 值 | 说明 |
|---|---|---|
| 信标间距（走廊） | ≤ 6m | 视障导航连续覆盖 |
| 指纹间距（走廊面） | 1.0m | 2 步内感知偏离并纠正 |
| 指纹走廊覆盖宽度 | ≤ 6m（中心线 ±3m） | 覆盖走廊全截面（左/中/右） |
| 楼梯/电梯口加密 | 3m 半径 / 1m 间距 | 关键过渡点高密度 |
| 安装方式 | 100% 墙面附着 | 墙装/门套装/柱装，0 天花吊杆 |

### 1.3 信标协议参数

| 参数 | 值 |
|---|---|
| UUID | `B9407F30-F5F8-466E-AFF9-25556B57FE6D` |
| 协议 | iBeacon（Apple 标准） |
| 广播间隔 | 门口/交叉口 400ms；楼梯/电梯 300ms |
| 发射功率 | 门口 -10dBm；交叉口 -10dBm；楼梯/电梯 -10dBm（设备档位无 -8/-12，统一取最近档 -10；详见 §1.4 与用户手册） |
| 电池 | CR2477 / CR2450（可更换），预期续航 > 6 个月 |

> **📌 发射功率档位影响说明**：本方案原计划按区域分档（门口 -8 / 交叉口 -10 / 楼梯电梯 -12 dBm），但 RF-B-SR1（及多数同类信标）实际档位仅有 `-28/-20/-10/-5/-3/0/1/2/4/6`，无 -8/-12。统一取最近档 **-10 dBm** 对本项目**影响可忽略**：
> - **覆盖无空洞**：-10 dBm 室内可用距离约 15–20 m，远超 6 m 信标间距 + 3 m 走廊指纹缓冲；
> - **指纹精度无损**：本项目为指纹定位（靠原始 RSSI 指纹库 + 拓扑路由，非三角定位），统一功率反而更一致、更易复现，原分档梯度在指纹法下几乎无收益；
> - **跨层串扰可忽略**：统一 -10 较原 -12 仅强 2 dB，而混凝土楼板衰减 15–30 dB，2 dB 被完全淹没；且跨层归属由拓扑层（`route_rules` 盲模式 / 楼层约束）处理，不依赖 RSSI；
> - **唯一动作**：部署时在 -10 dBm 下实测各信标 1 m 处 RSSI，写回 iBeacon 帧的 `RSSI@1m`（Measured Power）字段，供帧内距离估计使用（指纹法用原始 RSSI，影响很小）。
>
> 仅当改为纯三角定位、或信标间距拉到 15–20 m 以上开阔区时，档位差异才会产生可感知影响。

### 1.4 推荐信标型号与选型

基于学生科研项目、路线验证阶段（48 个信标）、室内视障导航场景，推荐如下型号。

#### 首选：Minew I9（nRF52832 方案）

| 项 | 详情 |
|---|---|
| 芯片 | Nordic nRF52832（BLE 5.0，iBeacon + Eddystone 双帧） |
| 电池 | CR2477（1000mAh，可更换） |
| 续航 | 广播间隔 400ms → 约 14–18 个月（远超半年底线） |
| 配置 | BeaconSET App 蓝牙直连，修改 UUID/Major/Minor/TxPower/Interval |
| 防护 | IP65（室内足够） |
| 安装 | 自带挂孔 + 3M 背胶，支持墙装/门套装 |
| 单价 | 品牌 **¥38–48/个** |

#### 预算版：RF-STAR 信驰达 RF-B-SR1（✅ 已确认，依据用户手册 docs/reference/RF-B-SR1_user_manual.pdf）

| 项 | 详情 |
|---|---|
| 型号 | RF-B-SR1 |
| 芯片 | Silicon Labs EFR32BG22（**BLE 5.2**） |
| 电池 | **CR2477**（可更换，标称 5.9 年 @ 1s/0dBm） |
| 续航 | 按本方案 300–400ms/-10dBm 估算约 1.5–2 年（远超 6 个月底线） |
| 协议 | iBeacon（默认），可切 Eddystone |
| 发射功率档位 | **-28/-20/-10/-5/-3/0/1/2/4/6 dBm**（无 -8/-12，部署统一取 -10） |
| 配置方式 | **手机 App 经 BLE 连接，写 AT 指令 UUID 下发 AT+ 命令**（无需串口线） |
| 尺寸 | 51×51×21 mm，ABS 外壳 |
| 接口 | UART（二次开发/OTA），终端用户走 BLE |
| 单价 | **< ¥30/个** |
| ✅ 采购前验证 | 4 项均已由手册确认：① UUID 可写（AT+BEACON 第5参数 16 字节）；② Major/Minor 可设（AT+BEACON 参数 2/3，2 字节前补零）；③ TxPower/Interval 可设（AT+POWER / AT+ADS，Interval 200ms~10.24s）；④ 8m 外稳定扫描（0dBm 室外>50m） |

#### 不推荐更低价位的原因

| 候选 | 单价 | 不推荐原因 |
|---|---|---|
| nRF51822 白牌 | ¥12–18 | BLE 4.2，不支持 Eddystone，App 兼容性差 |
| CR2032 纽扣型 | ¥15–22 | 电池仅 220mAh，400ms 广播 ≈ 4 个月（临界半年，风险高） |
| 不可配置型 | ¥8–12 | UUID/Major 出厂固化，无法匹配部署方案的编号体系 |

#### 采购建议

- **稳妥路线**：先买 **2–3 个 Minew I9 样品**验证 App 配置流程与扫描稳定性，确认后批量采购。
- **经济路线（✅ 已确认可行）**：RF-B-SR1 已通过手册验证，可替换为批量采购型号：
  1. ✅ 写入 UUID `B9407F30-F5F8-466E-AFF9-25556B57FE6D`（AT+BEACON 第5参数为 16 字节自定义 UUID）
  2. ✅ 分别设置 Major（楼层号）、Minor（beaconId 编号）（AT+BEACON 参数 2/3，2 字节 hex 前补零，如楼层 1→`0001`）
  3. ⚠️ TxPower 档位无 -8/-12，采用最近档 **-10 dBm**；Interval 可设 300/400 ms（AT+POWER / AT+ADS）
  4. ✅ 手机 nRF Connect 在 8 m 外稳定扫描（0dBm 室外>50m 传输距离）

---

### 1.5 定位体制选型（指纹 vs 三点定位）

本方案主选**指纹定位**（信标少、抗多径强、适合视障复杂室内，见 §二/§三）。若项目改为**三点定位（Trilateration）**，必须保证每个点被 ≥3 信标覆盖且功率一致：

- **全楼层三点定位**：信标数量 324 个，覆盖全楼 walkable 区，预算版约 ¥8,700，见 **§8 全楼层部署方案**。
- **沿测试路径三点定位**：仅保证两条测试路线（指纹网格 753 点）上 ≥3 覆盖，只需 **66 个信标**（48 路线基础 + 18 走廊补点），预算版约 **¥1,800**，见 **§8.7 轻量部署方案**。

三者互斥，按实际"硬件成本 vs 采集人工 vs 覆盖范围"权衡三选一。

---

## 二、信标安装方案（48 个）

### 2.1 分布概览

| 楼层 | 门口 | 交叉口 | 走廊填充 | 电梯 | 合计 |
|---|---|---|---|---|---|
| 1F | 3 | 32 | 7 | 2 | **44** |
| 2F | 1 | 2 | 1 | 0 | **4** |

### 2.2 安装方式分布

| 安装方式 | 数量 | 说明 |
|---|---|---|
| **wall**（墙装） | 39 | 走廊侧墙、楼梯间墙、电梯厅墙、房间外墙 |
| **door_frame**（门套装） | 9 | 门口侧套或门楣下 |
| ceiling_pendant（天花吊杆） | **0** | ✅ 无需天花部署 |

### 2.3 安装位置优先级

```
① 柱/门套（snap ≤ 2.5m）─ 最优先，结构柱或门框自然附着面
② 投影到最近墙线段（≤ 8m）─ 走廊侧墙、房间外墙
③ 天花吊杆 ─ 仅最后手段（当前方案 0 个）
```

### 2.4 各安装方式施工要点

#### 墙装（wall）— 39 个

- **安装高度**：地面以上 2.0–2.2m
- **位置要求**：
  - 走廊侧墙、楼梯前室墙、电梯厅侧墙、房间外墙
  - 距阴角（墙角转折处）≥ 0.3m
  - 避免正对消防栓、疏散指示灯、弱电箱正面（可靠近侧缘）
- **固定方式**：3M 建筑双面胶（VHB）直接粘贴，必要时加免钉胶加强，无需打孔
- **特殊要求**：若信标吸附到柱体，安装在**面向走廊一侧**，避免广告牌遮挡

#### 门套装（door_frame）— 9 个

- **安装高度**：门套侧面或门楣下方，2.0–2.2m
- **位置要求**：避开闭门器活动范围，选择门扇打开时不受撞击的一侧
- **固定方式**：优先 3M 建筑双面胶（门套表面不宜打孔），确保粘贴前清洁门套表面

#### 天花吊杆（ceiling_pendant）— 0 个（当前方案无需）

### 2.5 施工前准备清单

| 类别 | 物品 | 数量 | 用途 |
|---|---|---|---|
| 信标 | Minew I9 信标（nRF52832，详见第七章） | 55（48+7 备用） | 部署 + 15% 备件 |
| 电池 | CR2477 或 CR2450 | 55 枚 | 全部装新电池，确保续航一致 |
| 配置工具 | 手机（Android/iOS）+ BeaconSET App | 1 | 配置 UUID/Major/Minor/TxPower/Interval |
| 安装工具 | 无（全部采用 3M 双面胶免打孔固定） | — | 墙 / 门套 / 柱体均胶粘 |
| 安装辅材 | 3M VHB 双面胶（宽 1cm） | 2 卷 | 门套装临时固定、柱装辅助 |
| 测量工具 | 5m 卷尺、激光测距仪（**与 §6.3 地图勘测共用**） | — | 定位安装高度与图纸坐标匹配 |
| 标记工具 | 美纹纸、记号笔（**用学校现有或普通胶带替代，不单独采购**） | — | 临时标记安装位置 |
| 安全用品 | 人字梯（≥2m）、安全帽 | 各 2 | 向学校后勤借用，不采购 |
| 图纸 | 楼层平面图（打印）+ 信标位置表 | 各 2 份 | 现场定位依据 |
| 记录工具 | 手机拍照 + 纸质 checklist | — | 记录安装状态与偏差 |
| 数据文件 | `beacon_deployment_plan_routes.json` | — | 精确坐标与参数 |

### 2.6 安装流程（逐层推进）

```
第 1 步 — 打印图纸与信标位置表
    │  将楼层平面图打印为 A3 尺寸，在每个信标位置标注 ID
    │
第 2 步 — 按楼层从 2 层开始（信标少，先练习）
    │  2F 共 4 个信标 → 1F 共 44 个信标
    │
第 3 步 — 在 BeaconSET App 中预配置信标参数
    │  为每个 beaconId 设置 UUID / Major / Minor / TxPower / Interval
    │  Major = 楼层号, Minor = beaconId 编号
    │
第 4 步 — 现场定位 + 标记
    │  根据 JSON 中的 coordinates / locationDesc / mountType
    │  用卷尺或激光测距仪在墙上标记精确位置（美纹纸+记号笔）
    │
第 5 步 — 安装固定
    │  按 mountType 选择安装方式（墙装贴胶 / 门套贴胶，均免打孔）
    │
第 6 步 — 安装后验证
    │  手机打开蓝牙扫描工具（如 nRF Connect 或 BeaconSET）
    │  确认：UUID 正确 / RSSI 在 1m 处为 -55~-65dBm / 广播间隔符合设定
    │
第 7 步 — 拍照存档
    │  每信标拍 1 张「安装位置整体照」+ 1 张「信标特写」
    │  命名规则：BK-{楼层}-{序号}.jpg
```

> **预算版（RF-B-SR1）逐信标配置**：本方案已依据 `beacon_deployment_plan_routes.json` 生成完整的 AT 指令清单
> `docs/reference/beacon_at_commands_routes.md`（含 48 个信标的 `AT+NAME` / `AT+BEACON` / `AT+POWER=-10` / `AT+ADS=,1,300`）。
> 现场用 **nRF Connect** 连接每个信标，按清单逐条发送（AT 指令大写、不带回车换行、MTU≥128）；`AT+ADS` 模式务必保持 **1（可连接）**。
> RSSI@1m 当前为占位值 `C5`（-59 dBm），建议现场实测后替换（清单文末有校准步骤）。

### 2.7 关键信标位置说明

| beaconId | 楼层 | 位置描述 | 安装方式 | 安装提示 |
|---|---|---|---|---|
| BK-01-001 | 1F | 1F 电梯口（II-01#EL）· 门套或呼梯板旁 | door_frame | 避开电梯门运行轨道区域 |
| BK-01-002 | 1F | 1F 电梯厅中心·柱或侧墙 | wall | 电梯厅筒体柱面或侧墙 |
| 路线交叉口 | 1F | 走廊交叉口（34 个 intersection 节点） | wall | 尽量利用柱体，其次侧墙 |
| 走廊覆盖点 | 1F | 走廊中段填充（8 个 corridor 填充点） | wall | 两交叉口间距 > 6m 时补点 |

### 2.8 验收标准

- ✅ 所有 48 个信标均已安装到位、固定牢固
- ✅ 每个信标 UUID / Major / Minor / TxPower / Interval 与部署方案一致
- ✅ 信标广播可被手机在 8m 外稳定扫描（无障碍物时 RSSI ≥ -75dBm）
- ✅ 安装高度统一（2.0–2.2m），同一区域高度一致
- ✅ 无信标安装在消防栓正面、疏散指示灯正面、弱电箱正面
- ✅ 所有信标电池电压 ≥ 2.8V（新电池标准）
- ✅ 照片档案齐全

---

## 三、指纹采集方案（753 个点）

### 3.1 分布概览

| 楼层 | 普通点（走廊面） | 安全加密点（楼梯/电梯口） | 合计 |
|---|---|---|---|
| 1F | 710 | 7 | **717** |
| 2F | 29 | 7 | **36** |

### 3.2 采集参数

| 参数 | 值 | 说明 |
|---|---|---|
| 走廊面间距 | 1.0m | 视障导航标准：用户走 2 步跨越 1 个指纹点 |
| 楼梯/电梯口加密间距 | 1.0m | 关键过渡点高密度 |
| 加密半径 | 3.0m | 以楼梯/电梯节点为圆心的可通行区域 |
| 去重最小间距 | 0.6m | 小于此距离的点合并 |

### 3.3 采集前准备

| 类别 | 物品 | 数量 | 用途 |
|---|---|---|---|
| 采集工具 | 智能手机（Android/iOS）+ BLE 扫描 App | 1–2 部 | 采集 BLE RSSI 指纹 |
| 扫描 App | nRF Connect（Nordic）或自研采集 App | 1 | 支持 RSSI 批量采集和导出 |
| 标记工具 | 地面标记贴纸 / 粉笔 / 美纹纸 | 若干 | 在地面标记采集点位置 |
| 测量工具 | 5m 钢卷尺（**复用 §6.3 勘测卷尺，不重复采购**） | — | 辅助定位网格点 |
| 图纸 | 楼层平面图 + 指纹采集点地图 | 各 2 份 | 现场定位依据 |
| 记录表单 | 采集记录 checklist（纸质或电子） | 各 1 | 记录每个点的采集状态 |
| 信标 | **确认 48 个信标全部安装到位并正常广播** | — | 指纹采集前必须信标已部署 |
| 数据文件 | `fingerprint_grid_routes.json` | — | 精确坐标与采集点 ID |

### 3.4 采集流程

```
第 1 步 — 确认信标已全部部署并广播正常
    │  用手机在走廊两侧各扫一次，确认 48 个信标均可检测
    │
第 2 步 — 在地面标记采集网格点
    │  根据 fingerprint_grid_routes.json 中的 coordinates
    │  用卷尺从墙壁或柱体参考点定位，在地上贴标记贴纸
    │  标记格式：FP-{楼层}-R{序号}（如 FP-1-R001）
    │  按"列"推进：每列（走廊横向）标记完毕再移下一列
    │
第 3 步 — 逐点采集 RSSI 指纹
    │  手机置于标记点正上方，距地面约 1.2m（手持高度）
    │  面向走廊方向（统一朝向，减少人体遮挡偏差）
    │  静止 5 秒，采集期间手机位置不移动
    │  每点记录：扫描到的所有 beaconId + RSSI 值（通常取 3–5 次扫描的均值）
    │
第 4 步 — 采集记录格式
    │  每条指纹记录：
    │  {
    │    "fpId": "FP-1-R001",
    │    "floor": 1,
    │    "coordinates": [-41.065, 22.356],
    │    "timestamp": "2026-08-11T10:30:00+08:00",
    │    "scanDuration_ms": 5000,
    │    "rssi": [
    │      {"beaconId": "BK-01-010", "rssi": -58, "avg": -59.2},
    │      {"beaconId": "BK-01-011", "rssi": -65, "avg": -64.8}
    │    ]
    │  }
    │
第 5 步 — 逐层完成
    │  先完成 2F（36 点，约 15 分钟）作为热身
    │  再完成 1F（717 点，预计 2.5–3 小时）
    │
第 6 步 — 质量抽检
    │  随机选取 10% 的采集点（约 75 个）
    │  换一台手机重新采集，比对 RSSI 偏差
    │  若偏差 > 5dBm 的点超过 20%，检查环境干扰或信标状态
    │
第 7 步 — 数据导出与入库
    │  将采集结果导出为 JSON，放入 result/fingerprint_database.json
    │  导入到定位引擎（后续开发）
```

### 3.5 采集注意事项

| 注意事项 | 说明 |
|---|---|
| **朝向一致性** | 所有采集点统一面朝走廊方向（沿路线前进方向），避免人体朝向差异引入 RSSI 偏差 |
| **环境干扰** | 采集时走廊内避免人员走动；若无法清场，在采集记录中注明该点有人流干扰 |
| **金属干扰** | 电梯口、大型金属门附近 RSSI 波动较大（多径反射），每个点增加扫描时间到 8 秒 |
| **楼梯口加密点** | 7 个 safe 加密点位于楼梯间入口的**走廊侧**（非楼梯间内部），需确认标记位置在可站立区域 |
| **重叠点去重** | 若两个标记点间距 < 0.6m，按 JSON 中的 priority 字段优先保留 safe 点 |
| **设备校准** | 采集前后各选 1 个固定参考点，用同一手机在同一位置测 3 次 RSSI，确认设备未漂移 |

### 3.6 采集工作量估算

| 楼层 | 点数 | 预计时间 | 备注 |
|---|---|---|---|
| 2F | 36 | ≈ 15 min | 先做，熟悉流程 |
| 1F | 717 | ≈ 2.5–3h | 面积大但 1m 间距推进快 |
| **总计** | **753** | **≈ 3h** | 单人操作；双人可减半 |

---

## 四、施工与采集日程

### 建议分两阶段执行

```
阶段 1：信标安装（半天）
  ├── 上午 — 打印图纸、预配置 48 个信标参数
  ├── 下午 — 2F（4 信标）→ 1F（44 信标）逐层安装
  ├── 傍晚 — 全面扫描验收、拍照存档
  └── 产出：全部 48 个信标安装就位 + 验收通过

阶段 2：指纹采集（半天）
  ├── 上午 — 2F 地面标记 + 36 点指纹采集（热身）
  ├── 下午 — 1F 地面标记 + 717 点指纹采集
  ├── 傍晚 — 质量抽检（10% 约 75 点）+ 数据导出
  └── 产出：fingerprint_database.json + 抽检报告
```

---

## 五、档案交付物

| 交付物 | 位置 | 说明 |
|---|---|---|
| 信标部署方案 | `result/beacon_deployment_plan_routes.json` | 48 个信标的坐标/参数/安装方式 |
| 指纹采集方案 | `result/fingerprint_grid_routes.json` | 753 个采集点坐标与优先级 |
| 路线交互图 | `result/floor_layout_v9_interactive_routes.html` | 信标 + 指纹双图层叠加 |
| 本实施方案 | `docs/08-信标部署与指纹采集实施方案.md` | 施工流程与验收标准 |
| 信标配置参考 | `src/tools/gen_beacon_plan.py` | 配置参数定义与再生成命令 |
| 指纹配置参考 | `src/tools/generate_fingerprint_grid.py` | 指纹参数定义与再生成命令 |

### 再生成命令

```bash
# 信标方案
python src/tools/gen_beacon_plan.py \
    --mode route \
    --routes result/beacon_routes.json \
    --out result/beacon_deployment_plan_routes.json

# 指纹方案
python src/tools/generate_fingerprint_grid.py \
    --mode route \
    --routes result/beacon_routes.json \
    --out result/fingerprint_grid_routes.json

# 交互渲染图
python src/rendering/render_interactive.py \
    --beacons result/beacon_deployment_plan_routes.json \
    --fingerprint result/fingerprint_grid_routes.json \
    --out result/floor_layout_v9_interactive_routes.html
```

---

## 六、采购清单（设备选型与预算）

> 三类工具对应三个环节：**信标部署**（安装固定）、**指纹采集**（RSSI 采样）、**地图现场勘测**（CAD 与实建差异复核）。下列单价为学生科研采购区间，可按实际渠道浮动。

### 6.1 信标部署工具

| 物品 | 推荐型号 | 数量 | 单价 | 小计 | 说明 |
|---|---|---|---|---|---|
| **信标本体（品牌）** | Minew I9（nRF52832, CR2477） | 55 | ¥42 | ¥2,310 | BeaconSET App 配置，稳妥 |
| 信标配置 | 手机 + BeaconSET App | 1 | ¥0（已有） | ¥0 | Android / iOS |
| 安装辅材 | 3M VHB 双面胶（宽 1cm） | 2 卷 | ¥25 | ¥50 | 墙/门套/柱体通用免打孔固定 |
| 现场定位 | 5m 钢卷尺 + 激光测距仪（**与 §6.3 地图勘测共用，不重复采购**） | — | ¥0（共用） | ¥0 | 图纸坐标匹配，复用勘测工具 |
| 信标本体（预算版） | RF-STAR RF-B-SR1（CR2477） | 55 | ¥27 | ¥1,485 | 需先通过 4 项配置验证（见 1.4） |
| **小计（品牌）** | | | | **¥2,360** | 激光/卷尺复用 §6.3（免打孔、无冲击钻/人字梯） |
| **小计（预算版）** | | | | **¥1,535** | 激光/卷尺复用 §6.3（免打孔、无冲击钻/人字梯） |

### 6.2 指纹采集工具

| 物品 | 推荐型号 | 数量 | 单价 | 小计 | 说明 |
|---|---|---|---|---|---|
| 采集终端 | 智能手机（支持 BLE 5.0） | 1–2 部 | ¥0（已有） | ¥0 | 需支持后台扫描 |
| 扫描 App | nRF Connect / 自研采集 App | 1 | ¥0 | ¥0 | 支持 RSSI 批量导出 |
| 地面标记 | 地面标记贴纸 / 粉笔 | 若干 | ¥30 | ¥30 | 753 点定位 |
| 现场记录 | 平板 / 纸质 checklist | — | ¥0（已有） | ¥0 | 采集状态记录 |
| **小计** | | | | **¥30** | 终端复用已有；手机手持 1.2m 采集，无需支架 |

### 6.3 地图现场勘测工具

> 用于 CAD 平面图与实建差异复核（墙体位置、层高、门洞尺寸、走廊宽度），保证部署坐标与现场一致。

| 物品 | 推荐型号 | 数量 | 单价 | 小计 | 说明 |
|---|---|---|---|---|---|
| 激光测距仪 | 国产手持激光测距仪（深达威/优利德 40–60m） | 1 | ¥150 | ¥150 | 墙距/层高测量；**兼供 §6.1 信标现场定位**；实验精度足够 |
| 钢卷尺 | 5m | 1 | ¥30 | ¥30 | 短距复核（信标间距≤6m，5m 足够） |
| 拍照记录 | 数码相机 / 手机 | — | ¥0（已有） | ¥0 | 细部拍照 |
| 现场录入 | 平板 + 云笔记 | — | ¥0（已有） | ¥0 | 勘测数据录入 |
| **小计** | | | | **¥180** | 精简勘测配置（学生实验）：激光+5m卷尺，长廊/夹角用卷尺与手机测量 App 估算 |

### 6.4 预算汇总

| 类别 | 品牌信标（Minew I9） | 预算版（RF-B-SR1） |
|---|---|---|
| 信标部署（6.1） | ¥2,360 | ¥1,535 |
| 指纹采集（6.2） | ¥30 | ¥30 |
| 地图勘测（6.3，基础） | ¥180 | ¥180 |
| **合计** | **¥2,570** | **¥1,745** |

> 建议：先验样（2–3 个 Minew I9 或 RF-B-SR1 + 基础勘测工具）走通流程，再批量采购；本次采购不含全景相机。

---

## 七、常见问题

**Q: 为什么只在两条测试路线上部署，而非全楼？**
A: 这是科研项目的路线验证阶段。先在两条关键路线上验证信标 + 指纹的定位效果，方案成熟后可扩展到全楼（切换为 `--mode full` 即可生成全楼 264 信标 + 1442 指纹方案）。

**Q: 信标电池能用多久？**
A: 当前广播间隔 300–400ms、使用 CR2477 电池：Minew I9 预期 10–16 个月；RF-B-SR1 标称 5.9 年 @ 1s/0dBm，按本方案 300–400ms/-10dBm 估算约 1.5–2 年。均超过学生项目的半年最低要求。

**Q: 指纹采集为什么要统一手持高度 1.2m？**
A: 定位时用户手机也在手持高度；保持采集与定位高度一致可消除高度偏差引入的 RSSI 误差。

**Q: 有信标位置在 JSON 和图纸上与实际现场不对应怎么办？**
A: JSON 中的 `snapDist_m` 字段记录了信标从拓扑节点吸附到墙面/柱体的偏移量。若偏移 > 2m 导致实际位置不可用，记录偏差并在该走廊段两端的现有信标之间取中点补装一个替代信标（标注为现场调整），回写 JSON。

**Q: 设备不支持 -8/-12 dBm 档位、统一用 -10 dBm，对导航效果有何影响？**
A: **可忽略**，理由有四：① 覆盖——-10 dBm 室内可用距离约 15–20 m，远超 6 m 间距 + 3 m 走廊指纹缓冲，不会出死区；② 精度——本项目是指纹定位（原始 RSSI 指纹库 + 拓扑路由），统一功率更一致、更易复现，原分档梯度在指纹法下几乎无收益；③ 跨层——统一 -10 较原 -12 仅强 2 dB，混凝土楼板衰减 15–30 dB 将其淹没，且跨层归属由拓扑层处理而非 RSSI；④ 只需在 -10 dBm 下实测 1 m 处 RSSI 写回 iBeacon 帧的 Measured Power 字段。详见 §1.3 档位影响说明。

**Q: 能否改用三点定位（Trilateration）而不采集指纹？**
A: 可以，有两种量级可选：
1. **沿测试路径三点定位（§8.7）**：只覆盖两条测试路线（753 个指纹网格点），仅需 **66 个信标**（48 路线基础 + 18 走廊补点），仿真 100% 路径点 ≥3 覆盖，预算版约 **¥1,800**。
2. **全楼层三点定位（§8）**：覆盖全楼 walkable 区，需 **324 个信标**，仿真 F1 96.6% / F2 98.1% ≥3 覆盖，预算版约 **¥8,748**。

无论哪种，都能省去 753 点指纹采集人工；代价是信标数量与覆盖范围成正比。若项目只是想"不采指纹也能在两条路线上定位"，§8.7 的 66 信标方案最划算。

## 八、备选定位体制：三点定位（Trilateration）全楼层部署

> 本节给出"若采用三点定位而非指纹定位"时的全楼层信标部署方案。它与本方案主选的指纹定位（§二/§三）是**两种互斥体制**，按项目实际需要二选一，不要混用。

### 8.1 三点定位的原理与硬约束

三点定位（多边/三角定位）不依赖离线指纹库，而是用物理模型把信号强度反推为距离、再几何求交：

```
在线阶段：手机收到 ≥3 个信标 RSSI → 每个 RSSI 经路径损耗模型 d=10^((Tx−RSSI)/(10n)) 算距离
          → 以各信标为圆心画圆 → 三边（多边）交汇得到坐标
```

因此它对部署有两条硬约束，与指纹法截然不同：

1. **覆盖约束**：每个可行走点必须同时被 **≥3 个信标** 稳定接收（RSSI > −85 dBm）。这是几何求交的最低条件；少于 3 个则无法定位或误差剧增。
2. **功率一致性约束**：距离公式直接吃 `TxPower`，**所有信标必须同一发射功率**（本项目统一 −10 dBm，见 §1.3）。功率不一致 → 距离全偏。

> 关键差异：指纹法用"现场采样的真实 RSSI 查表"，不怕功率不一致、不怕多径；三点定位用"模型算距离"，怕信号失真、怕功率漂移、怕穿墙衰减。本项目主选指纹法正是因为它对视障复杂室内更鲁棒、且信标少。

### 8.2 全楼层部署方案（324 个信标）

以 `result/beacon_deployment_plan.json`（全楼 264 个，全贴墙/门套）为基础，针对覆盖约束做缺口填补后生成：

| 楼层 | 基础(贴墙/门套) | 房间内补点 | 合计 | 可行走面积 |
|---|---|---|---|---|
| F1 | 155 | 49 | **204** | 2440 m² |
| F2 | 109 | 11 | **120** | 1122 m² |
| **合计** | **264** | **60** | **324** | 3563 m² |

- **基础 264 个**：沿用全楼计划，全部 `wall` / `door_frame` 安装（2.2 m），UUID/Major/Minor 已规划。
- **补点 60 个**：用贪心算法对"可见信标 <3"的室内缺口点布放，坐标落在**房间内部**（保持与其他信标可视 LOS），建议贴房间内墙 / 高处书架 / 支架 2.2 m 安装，**仍避免天花板部署**（遵循 §1.1 视障部署原则）。`mountType=interior`，`installHeight=2.2`。
- **全部 324 个统一 `txPower=−10 dBm`、`broadcastInterval=300 ms`**（满足功率一致性约束）。

### 8.3 覆盖仿真结果（射线穿墙模型）

采用与 §1.3 一致的无线模型：TxPower −10 dBm、RSSI@1m 参考 −50 dBm、路径损耗指数 n=3.5、穿墙衰减 brick 12 / concrete 15 / partition 8 dB，可见阈值 −85 dBm。对全楼 walkable 区 1 m 网格采样（F1 2414 / F2 1127 点），逐点射线穿墙计算可见信标数：

| 楼层 | 填补前 ≥3 覆盖 | 填补后 ≥3 覆盖 | 剩余缺口点 |
|---|---|---|---|
| F1 | 69.22% | **96.64%** | 81 / 2414 |
| F2 | 89.35% | **98.14%** | 21 / 1127 |

填补后直方图（每点可见信标数）：F1 中 5 个可见占 1896 点、4 个占 323 点，说明**绝大多数点有 4~5 重冗余**；残余 ~4% 为房间贴墙角点（需第三信标穿墙才能到达），属工程可接受范围，可通过局部微调信标或对该类点降级为"走廊级覆盖"处理。

### 8.4 与指纹方案（主选）的对比

| 维度 | 指纹定位（主选，§二/§三） | 三点定位（本节） |
|---|---|---|
| 信标数量 | **48**（路线式） | **324**（全楼层） |
| 离线建库 | 753 点人工采集（约 6 h+） | 0（仅需信标标定） |
| 在线算力 | 全指纹库向量匹配 | 解三边方程（轻量） |
| 抗多径/穿墙 | **好**（直接存真实信号） | 差（穿墙使距离算错） |
| 功率一致性要求 | 不重要 | **必须一致（统一 −10）** |
| ≥3 覆盖 | 不适用 | 96.6% / 98.1% |
| 新增区域扩展 | 难（需重新踩点） | 易（补几个信标坐标） |
| 信标硬件成本(预算版) | ¥1,485 | **¥8,748** |
| 适合场景 | 视障复杂室内、精度优先 | 需实时坐标、无采集条件、区域常变动 |

### 8.5 预算影响（预算版 RF-B-SR1）

| 项 | 数量 | 单价 | 小计 |
|---|---|---|---|
| 信标本体（RF-B-SR1） | 324 | ¥27 | ¥8,748 |
| 3M VHB 双面胶（按 324 计） | 324 片 | ≈¥0.9 | ¥300 |
| 激光测距仪 + 5m 卷尺（复用 §6.3） | 1 套 | — | ¥180（共用） |
| **信标部署合计** | | | **≈ ¥9,228** |
| 指纹采集（§6.2，三点定位不需要） | 0 | — | ¥0（省去 753 点人工） |

> 若走品牌 Minew I9（¥42/个），信标本体升至 ¥13,608，部署合计约 ¥14,088。相较主选指纹方案（总预算 ¥1,745）硬件成本约 **6.7 倍**，但节省了全部指纹采集人工。

### 8.6 交付物与再生成

| 文件 | 说明 |
|---|---|
| `result/beacon_deployment_plan_trilateration.json` | 三点定位全楼层部署台账（324 信标，统一 −10 dBm） |
| `result/trilateration_coverage_analysis.json` | 覆盖仿真报告（前后对比、直方图） |
| `docs/reference/beacon_at_commands_trilateration.md` | 324 信标逐条 AT 配置命令（nRF Connect 烧录用） |
| `src/tools/gen_trilateration_plan.py` | 缺口填补 + 生成脚本 |
| `src/tools/analyze_trilateration_coverage.py` | 覆盖仿真脚本（可调整 `--grid` / `--threshold`） |

```bash
# 重新生成三点定位计划与覆盖仿真
python src/tools/gen_trilateration_plan.py --fill-cap 260 --grid 1.0
# 重新生成 AT 命令清单
python src/tools/gen_beacon_at_commands.py --src result/beacon_deployment_plan_trilateration.json \
    --out docs/reference/beacon_at_commands_trilateration.md --title-tag "三点定位全楼层部署 · 324 个"
```

### 8.7 沿测试路径三点定位轻量部署（66 个信标）

> 如果项目希望"不采指纹、仅在两条测试路线上做三点定位"，可采用本节轻量方案。它覆盖的区域与 §三的指纹采集路线完全一致（`result/fingerprint_grid_routes.json`，753 个路径点），但改为用 ≥3 信标覆盖这些点，因此信标数介于"主选指纹 48"与"全楼三点 324"之间。

#### 8.7.1 设计思路

- **测试路径** = `result/beacon_routes.json` 定义的两条路线：
  - 1F 音乐教室 → 组织办公（`F1-TR-0015` → `F1-TR-0036`）
  - 跨层 教师办公室 → 历史教室（`F1-TR-0033` → `F2-TR-0018`）
- 再加上 `generate_fingerprint_grid.py --mode route` 生成的 **3 m 走廊缓冲区**，共 **753 个目标点**（F1 717 / F2 36）。
- 以 `beacon_deployment_plan_routes.json`（48 个路线信标）为基础。
- 对每个目标点做射线穿墙 RSSI 仿真，标记可见信标 <3 的缺口。
- 用贪心算法沿走廊补点，补点吸附到最近走廊墙，保证 LOS（可视）。
- 全部信标统一 **`txPower=−10 dBm`**、**`broadcastInterval=300 ms`**。

#### 8.7.2 部署结构

| 楼层 | 基础路线信标 | 走廊补点 | 合计 |
|---|---|---|---|
| F1 | 44 | 14 | **58** |
| F2 | 4 | 4 | **8** |
| **合计** | **48** | **18** | **66** |

- **基础 48 个**：即 §二路线部署方案中的 `wall` / `door_frame` 信标（2.2 m）。
- **补点 18 个**：`mountType=wall`，贴走廊墙 2.2 m，非天花板；beaconId 前缀 `BK-TR-F1-xxx` / `BK-TR-F2-xxx`。

#### 8.7.3 覆盖仿真结果

物理模型与 §8.3 一致：TxPower −10 dBm、RSSI@1m 参考 −50 dBm、路径损耗指数 n=3.5、穿墙衰减 brick 12 / concrete 15 / partition 8 dB，可见阈值 −85 dBm。目标点即 753 个指纹网格点：

| 楼层 | 填补前 ≥3 覆盖 | 填补后 ≥3 覆盖 | 路径点 |
|---|---|---|---|
| F1 | 83.54% | **100%** | 717 |
| F2 | 66.67% | **100%** | 36 |

- F1 全部 717 个路径点都有 ≥3 个信标；F2 全部 36 个路径点也都有 ≥3 个。
- 大多数走廊点可见 4~5 个信标（冗余充足），拐角/端点可见 3 个。

#### 8.7.4 三种方案对比

| 维度 | 指纹定位（主选 §二/§三） | 三点定位·路线版（本节） | 三点定位·全楼层（§8.2） |
|---|---|---|---|
| 覆盖范围 | 两条测试路线 | 两条测试路线 | 全楼 walkable |
| 信标数量 | **48** | **66** | **324** |
| 离线指纹采集 | 753 点 | **0** | 0 |
| 在线算力 | 全库匹配 | 解三边方程 | 解三边方程 |
| 抗多径/穿墙 | **好** | 一般（限走廊） | 一般 |
| ≥3 覆盖 | 不适用 | **100% 路径点** | 96.6% / 98.1% |
| 预算版整项目成本 | 约 **¥1,745**（含勘测工具） | 约 **¥2,022**（见 8.7.5） | 约 **¥9,228** |
| 适用场景 | 视障精度优先 | 不采指纹、仅测两条路线 | 不采指纹、覆盖全楼 |

> 路线版三点定位的硬件成本略高于主选指纹方案（因要多 18 个补点信标），但省去了全部 753 点指纹采集工作。若人工采集时间紧张，这是性价比最高的"不采指纹"方案。

#### 8.7.5 预算影响（预算版 RF-B-SR1）

| 项 | 数量 | 单价 | 小计 |
|---|---|---|---|
| 信标本体（RF-B-SR1） | 66 | ¥27 | ¥1,782 |
| 3M VHB 双面胶 | 66 片 | ≈¥0.9 | ¥60 |
| 激光测距仪 + 5m 卷尺（复用 §6.3） | 1 套 | — | ¥180（共用） |
| **信标部署合计** | | | **≈ ¥2,022** |
| 指纹采集（三点定位不需要） | 0 | — | ¥0（省去 753 点人工） |

建议额外备 5 个信标作为替换/现场微调，含备件后约 **¥2,157**。

#### 8.7.6 交付物与再生成

| 文件 | 说明 |
|---|---|
| `result/beacon_deployment_plan_trilateration_routes.json` | 路线三点定位部署台账（66 信标，统一 −10 dBm） |
| `result/trilateration_coverage_routes_analysis.json` | 覆盖仿真报告 |
| `result/trilateration_coverage_routes_map_F1.png` / `_F2.png` | F1/F2 路径覆盖热力图 |
| `docs/reference/beacon_at_commands_trilateration_routes.md` | 66 信标逐条 AT 配置命令（nRF Connect 烧录用） |
| `src/tools/gen_trilateration_plan_routes.py` | 路线三点定位生成脚本 |
| `src/tools/gen_trilateration_map_routes.py` | 路线覆盖热力图脚本 |

```bash
# 重新生成路线三点定位计划与覆盖仿真
python src/tools/gen_trilateration_plan_routes.py --fill-cap 150 --grid 1.0
# 重新生成热力图
python src/tools/gen_trilateration_map_routes.py
# 重新生成 AT 命令清单
python src/tools/gen_beacon_at_commands.py --src result/beacon_deployment_plan_trilateration_routes.json \
    --out docs/reference/beacon_at_commands_trilateration_routes.md --title-tag "沿测试路径三点定位部署 · 66 个"
```

