# 审查结论解决日志 2026-08-12

- 日期：2026-08-12
- 分支：`review-fixes-2026-08-12`
- 审查依据：[docs/code-review-2026-08-12.md](./code-review-2026-08-12.md)（21 项问题：A1-A6、B1-B10、C1-C4、D1-D6、E1-E5）
- 本次目标：按审查结论拆分为任务逐项解决，P0/P1 中低风险项优先，高风险大重构（B1/B2、A2）明确延后。

---

## 一、任务拆分与解决状态总表

| 任务 | 审查项 | 内容 | 状态 | 说明 |
|------|--------|------|------|------|
| T-01 | A1 | `_merge_nearby_doors` 引用未定义变量 `centers` | ✅ 已解决 | 修正为 `coords`，并加 DISABLED 标注 |
| T-02 | A3 | `build_cross_floor_edges` 死 import，两套跨层实现并存 | ✅ 已解决 | 移除多余 import，标注并存关系 |
| T-03 | A4 | `_heban_real_polygon` v1 死代码 | ✅ 已解决 | 整段删除，保留 v2 射线投票版 |
| T-04 | A5 | `LAYERS_FURNITURE = ()` 空配置死代码链 | ✅ 已解决 | 标注 DISABLED 配置与恒空收集循环 |
| T-05 | A6/D1-D4 | 常量多处裸写/重复定义 | ✅ 已解决 | 新建 `src/common/constants.py` 统一来源 |
| T-06 | B6 | 函数体开头重复 docstring | ✅ 已解决 | 合并为一个 docstring |
| T-07 | B7 | 单元素列表做计数器 | ✅ 已解决 | 改用 `itertools.count` |
| T-08 | B8 | 被禁用逻辑无系统标注 | ✅ 已解决 | 新建 `docs/设计决策记录.md`（ADR-01/02）+ 各函数 DISABLED 标注 |
| T-09 | B9 | 注释与「门不合并」约定不一致 | ✅ 已解决 | 注释改为「每扇门独立成 TD」 |
| T-10 | B10 | 穿墙几何判定三份同源实现 | ✅ 已解决 | 收敛到 `src/geometry/segments.py` |
| T-11 | C1 | `_dijkstra` 列表+全量 sort 模拟优先队列 | ✅ 已解决 | 改用 `heapq` |
| T-12 | C2 | `_edge_by_id` 线性扫描 | ✅ 已解决 | 建 `edge_by_id` 字典索引，O(1) 查询 |
| T-13 | E1 | 函数内 `import math as _m` | ✅ 已解决 | 移除，使用模块头 import |
| T-14 | E2/E3 | 「常开防火门 penalty=0」三处重复 / 三目链猜门节点 | ✅ 已解决 | 提取 `_door_penalty` / `_door_node_of_edge` |
| T-15 | E4 | 任务代号注释缺文档索引 | ✅ 已解决 | 注释引用 `docs/task.md` 与文档标题 |
| T-16 | E5 | 局部 `_bez_mid` 与模块级 `bezier_mid` 混用 | ✅ 已解决 | 统一为模块级 `bezier_mid` |
| T-17 | 附带 | render_interactive.py 嵌套引号 f-string（PEP 701，Py3.10 不支持） | ✅ 已解决 | L1480/L1624 重构为预计算变量 |

**本轮明确延后（不在本分支范围）**：A2（路由规则三处实现收敛，需先补 golden 回归测试）、B1（巨型单文件拆分）、B2（building_outline 依赖倒挂）、B3（sys.path 副作用）、B4（MANUAL_SKELETON 全局）、B5（图纸配置外置）、C3（O(n²) 近邻搜索）、C4（route extras 全量重算）、D5（跨层边魔法数字）、D6（TITLE_BLOCK_X）。理由见审查报告第三节：B1/B2/A2 收益大但回归风险高，建议先补「输入 PDF → 期望 GeoJSON 断言」的 golden 测试再逐步推进。

---

## 一·补（b1-split-parse 分支，2026-08-13）B5/D5/D6 图纸配置外置

| 审查项 | 内容 | 状态 | 说明 |
|------|------|------|------|
| B5 | 图纸配置外置（PDF 路径/输出路径/场馆元信息） | ✅ 已解决 | 新建 `src/common/drawing_config.py` 的 `DrawingConfig`；main() 支持 `--pdf-f1/--pdf-f2/--output/--venue-id/--venue-name/--version` CLI 覆盖 |
| D5 | 跨层边魔法数字（distance=4.2、estimatedTime=60/15、accessibilityLevel=999/0、riskLevel=10/1） | ✅ 已解决 | 抽为 `geojson_writer.py` 命名常量 `CROSS_FLOOR_*`（注明单位与语义） |
| D6 | `TITLE_BLOCK_X = 2900.0` | ✅ 已解决 | 默认值移入 `drawing_config.py`，`parse_floor` 经 cfg 贯穿到标签/编号提取；CLI `--title-block-x` 覆盖 |
| B3 | `sys.path` 导入副作用 | ✅ 已解决 | `parse_cad_pdf.py` 移除模块级 sys.path 插入（正式运行改用 `pathai-parse` / `python -m src.parsing.parse_cad_pdf`）；`render_interactive.py` 同类副作用一并清理并更新 docstring 用法 |

- 验证：py_compile 全过；30 项快速测试通过；管线重跑输出与 HEAD 源码**逐字节一致**（唯一差异为 B1 已引入的 `generator` 元数据字段）；golden 回归（test_pipeline_golden，490s）通过。
- 说明：`PDF_F1/PDF_F2/OUT_GEOJSON` 模块常量保留为默认值（golden 测试引用），`DEFAULT_CONFIG` 由它们构造，换楼经 CLI 覆盖即可不改源码。
- B3 补充验证（2026-08-13）：48 项测试通过；`python -m src.parsing.parse_cad_pdf --help` 与 `python -m src.rendering.render_interactive` 端到端渲染均正常。

---

## 二、逐项解决详情

### T-01（A1）`_merge_nearby_doors` NameError 修复
- 文件：`src/topology/topology.py`
- 改动：函数体 `ci = centers[i]` / `cj = centers[j]` → 使用签名中已定义的 `coords`（`coords` 为 None 时回退推导，逻辑与 pipeline.py 版本对齐，消除两处分叉）。
- 附加：函数 docstring 标注 `DISABLED`（ADR-01 门不合并），禁止任何调用链启用。

### T-02（A3）跨层边两套实现
- 文件：`src/parsing/parse_cad_pdf.py`
- 改动：从 `from src.topology import (...)` 移除未使用的 `build_cross_floor_edges`；在 import 处注释说明两套实现并存（本文件内嵌「图纸井道编号配对」版为实际使用，topology 几何配对版为历史版本），指向审查报告 A3。

### T-03（A4）删除 `_heban_real_polygon` v1
- 文件：`src/parsing/parse_cad_pdf.py`
- 改动：删除形态学版 `_heban_real_polygon`（原约 100 行死代码）。实际生效的 v2 射线投票版保持不变。

### T-04（A5）`LAYERS_FURNITURE` 空配置死代码链
- 文件：`src/parsing/parse_cad_pdf.py`
- 改动：`LAYERS_FURNITURE = ()` 标注 `# DISABLED: A-METAL-S 已迁至 LAYERS_STRUCT，不再单独作为家具层`；在 `furn_segs` 收集循环处加注释说明该循环恒不执行、`furn_segs` 恒空（死代码链）。保留参数链避免大改，待 B1 拆分时一并清理。

### T-05（A6/D1-D4）常量统一
- 新建：`src/common/__init__.py`、`src/common/constants.py`
- `constants.py` 导出：`SCALE=0.0529`、`ORIGIN_X=2019.1`、`ORIGIN_Y=1154.8`、`BLIND_WALK_SPEED=0.8`、`NORMAL_WALK_SPEED=1.2`、`DOOR_PENALTY={'swing':0.0,'fire':0.5,'opening':1.0}`
- 替换位置：
  - `src/parsing/parse_cad_pdf.py`（`attach_elevator_door_nodes` 内裸写 `0.8`、`SCALE`/`ORIGIN_*`）
  - `src/skeleton/pipeline.py`（裸写 `* 0.0529`、`BLIND_WALK_SPEED = 0.8` 删除）
  - `src/topology/topology.py`（`BLIND_WALK_SPEED = 0.8` 删除、裸写 `* 0.0529`）
  - `src/topology/route_rules.py`（`DOOR_PENALTY` 引用统一来源）
  - `src/rendering/render_interactive.py`（`DOOR_PENALTY` 引用统一来源，JS 端常量仍内嵌，属 A2 范畴延后）
  - `src/tools/field_survey_calibrate.py`、`src/tools/merge_manual_edges.py`（try/except ImportError 兜底：可独立运行，导入失败时回退同值，注释标明统一来源）

### T-06（B6）重复 docstring
- 文件：`src/skeleton/pipeline.py`
- 改动：`build_skeleton_topology` 内连续两个内容重叠的 docstring 合并为一个。

### T-07（B7）列表计数器 → `itertools.count`
- 文件：`src/topology/topology.py`
- 改动：`edge_seq = [0]` → `edge_seq = itertools.count(1)`；`edge_seq[0] += 1` → `seq = next(edge_seq)`。移除「单元素列表模拟可变计数器」的绕风格。

### T-08（B8）禁用逻辑系统标注
- 新建：`docs/设计决策记录.md`（ADR）
  - **ADR-01 门不合并**（铁律）：每扇门独立成 TD，禁止任何形式门合并；登记 `dedupe_doorways`、`_merge_nearby_doors`（topology + pipeline）为 DISABLED。
  - **ADR-02 手动骨架优先于自动中轴**：`skeleton_manual_parsed.json` 存在时跳过中轴提取。
- 标注更新：
  - `src/parsing/parse_cad_pdf.py` `dedupe_doorways`：docstring 标注 DISABLED + 引用 `docs/设计决策记录.md ADR-门不合并`
  - `src/topology/topology.py`、`src/skeleton/pipeline.py` `_merge_nearby_doors`：docstring 标注 DISABLED + 引用 ADR-01 与 `docs/项目迭代日志.md` 铁律

### T-09（B9）校验注释对齐
- 文件：`src/qa/validate_geojson.py`
- 改动：L97 注释「同开口的摆弧/防火/门洞合并为一个 TD」→ 改为与约定一致的表述（每扇门独立成 TD，不合并）。

### T-10（B10）穿墙判定收敛
- 新建：`src/geometry/__init__.py`、`src/geometry/segments.py`
- `segments.py` 提供：
  - `side(p, a, b)`：叉积符号判定
  - `segments_properly_cross(p1, p2, a, b, eps=1e-9)`：真正「穿透」墙线判定（两端点位于墙线两侧且交点落在线段内；共线/同侧不算）
- 改造：
  - `src/topology/route_rules.py` `_segment_crosses_wall` → 直接委托 `segments_properly_cross`
  - `src/rendering/render_interactive.py` `_seg_crosses_wall` → try/except ImportError 委托 `segments_properly_cross`（独立运行兜底保留本地实现）

### T-11（C1）`_dijkstra` 换 heapq
- 文件：`src/topology/route_rules.py`
- 改动：`pq.sort(key=...)` + `pq.pop(0)` + `pq.append(...)` → `heapq.heappush(pq, (nd, nb))` + `heapq.heappop(pq)`。总复杂度由 O(V² log V) 降至 O(E log V)。

### T-12（C2）`_edge_by_id` 索引
- 文件：`src/topology/route_rules.py`
- 改动：`_build` 内建 `self.edge_by_id = {e["id"]: e for e in self.edges}`；`_edge_by_id` 改为字典 `.get()` 查询，O(E) → O(1)。

### T-13（E1）移除函数内 import
- 文件：`src/topology/route_rules.py`
- 改动：`_add_doorless_toilet_links` 内 `import math as _m` 删除，改用模块头已导入的 `math`。

### T-14（E2/E3）门权重判定收敛
- 文件：`src/topology/route_rules.py`
- 新增辅助函数：
  - `_door_node_of_edge(e)`：从边两端猜门节点（消除三目链重复）
  - `_door_penalty(node, default=9.0)`：处理 `isNormallyOpen` → 常开防火门 penalty=0
- 三处重复的「常开防火门无惩罚」逻辑（`_build` 的 `best_door`、`_edge_weight`、door_filter）统一调用 `_door_penalty`。

### T-15（E4）任务代号注释索引
- 文件：`src/qa/validate_geojson.py`
- 改动：首行「T12: GeoJSON 拓扑质量校验」→ 补充「任务索引见 docs/task.md；验收标准见《公共空间识别方案》第八章」。其余模块 docstring 中的 T 代号由 `docs/task.md` 统一索引。

### T-16（E5）`bezier_mid` 统一模块级
- 文件：`src/parsing/parse_cad_pdf.py`
- 改动：模块级新增 `bezier_mid(bz)`；删除 `detect_doors` 内局部 `bezier_mid` 与 `parse_floor` 内局部 `_bez_mid`；两处调用（`fire_arc_near_wall`、`ordinary_door_arc`）统一使用模块级 `bezier_mid`。

### T-17（附带）PEP 701 f-string 修复
- 文件：`src/rendering/render_interactive.py`
- 背景：环境 Python 3.10.11（< 3.12），不支持 PEP 701 嵌套同类引号 f-string；`py_compile` 编译期报错。
- 改动：
  - L1480：`{_rid or ''}` → `{_rid or ""}`（消除单引号嵌套）
  - L1624：内联 `f"电梯门（{_elev_id or '?'}）"` → 预计算变量 `_elev_tip = f"电梯门（{_elev_id or '?'}）"` 后引用

---

## 三、验证结果

### 1. 语法编译（全量）
```
python -m py_compile src\common\constants.py src\common\__init__.py src\geometry\__init__.py
  src\geometry\segments.py src\topology\topology.py src\skeleton\pipeline.py
  src\parsing\parse_cad_pdf.py src\topology\route_rules.py
  src\rendering\render_interactive.py src\qa\validate_geojson.py
  src\tools\field_survey_calibrate.py src\tools\merge_manual_edges.py
```
结果：**COMPILE_EXIT=0，全部通过**（含 T-17 两处 f-string 修复后的全量复核）。

### 2. 冒烟测试（纯模块，无第三方依赖）
- `src.common.constants`：`SCALE=0.0529 / ORIGIN_X=2019.1 / ORIGIN_Y=1154.8 / BLIND_WALK_SPEED=0.8 / NORMAL_WALK_SPEED=1.2 / DOOR_PENALTY={'swing':0.0,'fire':0.5,'opening':1.0}` 导出正确。
- `src.geometry.segments.segments_properly_cross`：4 组断言通过（对角穿越=True、共线=False、平行=False、T 形接触=True）。
- 说明：沙箱环境缺 `shapely`（既有环境限制，与本次改动无关），`topology/route_rules/pipeline` 等依赖 shapely 的模块无法在此导入；正式运行环境依赖齐全（见 `requirements.txt`/`pyproject.toml`），本次改动均为纯语法/结构级修改，已通过 py_compile 全量验证。

### 3. 行为一致性检查
- 路由规则三层（后端 Python / 渲染端预计算 / 前端 JS）中，本轮仅收敛了 Python 侧重复（E2/E3、B10）；JS 端常量与 Dijkstra 复刻属 A2，已登记延后，未改动其行为。

---

## 四、变更文件清单

| 变更 | 文件 |
|------|------|
| 修改 | `src/parsing/parse_cad_pdf.py` |
| 修改 | `src/qa/validate_geojson.py` |
| 修改 | `src/rendering/render_interactive.py` |
| 修改 | `src/skeleton/pipeline.py` |
| 修改 | `src/topology/route_rules.py` |
| 修改 | `src/topology/topology.py` |
| 修改 | `src/tools/field_survey_calibrate.py` |
| 修改 | `src/tools/merge_manual_edges.py` |
| 新增 | `src/common/__init__.py`、`src/common/constants.py` |
| 新增 | `src/geometry/__init__.py`、`src/geometry/segments.py` |
| 新增 | `docs/code-review-2026-08-12.md`（审查结论） |
| 新增 | `docs/设计决策记录.md`（ADR） |
| 新增 | `docs/审查解决日志-2026-08-12.md`（本日志） |

---

## 五、遗留与后续建议

1. **A2 路由规则三处实现收敛** 是审查结论中风险最高项，建议下一分支处理：先补「同一图三处路径断言一致」的 golden 对拍测试，再收敛常量/规则数据唯一来源。
2. **D5 跨层边魔法数字**（distance=4.2、estimatedTime=60/15、accessibilityLevel=999/0、riskLevel=10/1）与 **D6 TITLE_BLOCK_X**：随 B5 图纸配置外置一并处理。
3. **B1 巨型文件拆分**：以先补解析结果回归测试为前提，逐层推进（`src/geometry/` 已先行建立，作为拆分落点）。
4. **提交纪律**：本分支改动按主题提交（见提交记录），合入 master 前按铁律确认 `git branch --show-current`。
